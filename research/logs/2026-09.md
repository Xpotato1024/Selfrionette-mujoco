---
status: historical
owner: research
last_verified: 2026-09-13
canonical_for: []
related:
  - research/README.md
  - docs/contracts/physical-limit-resolution.md
  - docs/contracts/physical-safety-envelope.md
---

# 2026年9月 研究・実装ログ

## 2026-09-07: R7-J-P2 physical limitのidentity不変条件を補正

- 日付: 2026-09-07
- 目的: P2のphysical limit resolutionが、placeholder identityをconcreteなjoint / motor / actuator relationとして扱わないことをP1のtyped limit boundaryまで一貫させる。
- 実施内容: `PhysicalLimit.name`をconstructor、deep validator、strict JSON decoderで共通のconcrete identity validatorへ接続した。validatorはoverridableなstr subclassを評価せずbuilt-in `str`だけを受け付ける。共有text validatorも同じ型境界をsource ID、revision、evidence referenceへ適用し、authoritative sourceのplaceholder検査を迂回できないようにした。envelope lookupとresolved-bounds lookupのqueryも同じvalidatorを通し、nested limitのbypassとdownstream resolutionがauthorityへ到達しないようにした。`unknown` / `unavailable`はconcreteなlimit nameとtyped status / reason / provenanceの組み合わせで保持する。
- 検証: placeholder variantsのdirect constructor、bypass後のvalidator / envelope、strict JSON decode、envelope / result / provider lookup、resolver downstreamをsoftware-only focused regressionで確認した。実機、physical numeric measurement、serial、OSC、robot outputは実行していない。
- 現在できるようになったこと: physical limit identity、envelope serialization / lookup、P2 resolution queryが同じplaceholder vocabularyを共有し、unknown evidenceをidentityの代用にせずfail-closedで扱える。
- 実験的価値: software projectionとphysical evidenceのidentity boundaryを再現可能に検証でき、未解決sourceをconcrete relationやauthoritative numeric boundへ昇格させる経路を狭めた。
- まだ言えないこと: authoritative physical numeric evidenceは0件のままである。mechanical stop、cable constraint、self-interference、environment clearance、dynamic feasibility、physical safety、実機動作は未検証である。
- 判断: existing bounds、unit、frame、provenance、seal、conversion、status semanticsは変更せず、`docs/experiment-notes/`は新しいexperiment conditionや観測結果を取得していないため更新しない。
- 未解決事項: P3以降のcollision / trajectory / output gateは別Issueのscopeであり、この修正では実装しない。
- 次の作業: stack全体のcanonical validationと独立reviewを実行する。
- 関連Issue / PR / commit: Issue #504、PR #522。current headとCI stateはGitHub metadataを正とする。

## 2026-09-13: #513 physical outputへP5 safety decisionを結合

- 日付: 2026-09-13
- 目的: R7-J P5のsafety decisionを特定のphysical output intent、candidate、Robot、software revision、freshnessへ結合し、explicit allowだけをlifecycleでsendableとして扱う。
- 実施内容: 新しい`runtime.output.safety_gate`がcanonical request bytesとvalidated P2/P3/P4 DTOのcontent digest、candidate、decision projection、checked-at、Robot / revision identityをbindする。既存P5 evaluatorとvalidatorを再利用し、safety formulaやP2/P3/P4に含まれないqpos / trajectory値は複製しない。`PhysicalOutputSendableRequest`はallow専用とし、lifecycle v2 eventへsafety status / reason / provenanceとrequest / safety-input / decision / binding digestを記録する。legacy v1 lifecycle eventsはread可能に保った。
- 検証: safety gate、lifecycle transition、v1 trace readback、runtime ownership / output boundary focused testsで61 passed、exit status 0を確認した。canonical P5 core、output trace、request contract regressionでは109 passed、exit status 0を確認した。検証はsoftware-onlyで、実機、serial、OSC、network送信、robot outputは実行していない。
- 現在できるようになったこと: output lifecycleはraw intentを受理せず、requestとfresh P5 allow bindingをともに満たしたtyped wrapperだけをactive stateへ受理する。hold / unavailable / reject / stop / invalid / stale / identity mismatchでは直前のsendable stateを破棄し、safety reasonとprovenanceをlifecycle evidenceへ残す。
- 実験的価値: software safety decisionとoutput intentのidentity / freshness / provenance結合を、request bytes、candidate、typed upstream evidence、lifecycle JSONLから再検証できる。これはphysical safety測定やactuation実績ではない。
- まだ言えないこと: upstream P2/P3/P4 DTOに含まれないraw qpos / trajectory値をこのgateは独立再構築しない。actual transport、physical numeric measurement、実機安全性、送信・acknowledgementは未検証である。
- 判断: output lifecycleの研究利用上のcapabilityとfail-closed条件が変わるため当月research logを更新した。実験条件・physical fixture・観測結果は新規取得していないため`docs/experiment-notes/`は更新しない。
- 未解決事項: external consumer / transport integrationとhardware validationはこのIssueの範囲外であり、この変更からは主張しない。
- 次の作業: exact-head reviewとCIはrootが管理する。physical output、socket、OSC、serialの実行は行わない。
- 関連Issue / PR / commit: Issue #513。

## 2026-09-13: #513 candidate identityをrequestへ固定

- 日付: 2026-09-13
- 目的: typed P5 safety evidenceを、そのevidenceが検査したcandidateと同一のphysical output requestだけへ結合する。
- 実施内容: `physical_output_candidate_id()`でcanonical request bytes由来のversion付きIDを定義し、`SafetyInput.candidate_id`と`SafetyDecision.candidate_id`の両方が一致する場合だけoutput bindingを許可する。不一致はIDを補完せず、status `rejected` / 送信可能でない状態として扱う。生成側はrequestを固定し、同じcommand / trajectoryを上流checkerへ渡す。P2/P3/P4 DTOはtyped safety evidenceを保持するがraw qpos / trajectory全体を保持しないため、output gateはそれらを再構築しない。
- 検証: candidate hash、別commandへsafe evidenceを再利用した場合のreject、SafetyInput / SafetyDecisionのcandidate ID不一致、active後のmismatch時にlatest request / sendableをclearするfocused runtime / architecture testsで66 passed、exit status 0を確認した。P5 core、output trace、request contract regressionでは109 passed、exit status 0を確認した。実機、serial、OSC、network送信、robot outputは実行していない。
- 現在できるようになったこと: canonical request identityとP5 evidenceのcandidate identityが同じ場合だけlifecycleへsendable requestを渡せる。
- 実験的価値: command identityの不一致をsoftware回帰テストで再現できる。ただし生成側が同じcommand / trajectoryをcheckerへ渡す意味上の保証は上流の責務であり、独立したphysical measurementではない。
- まだ言えないこと: upstream DTOに残らないraw qpos / trajectoryの再構築、actual transport、physical numeric measurement、実機安全性、送信・acknowledgementは未検証である。
- 判断: #513のcandidate binding仕様が追加されたため当月research logを追記した。実験条件や実機観測は新規取得していないため`docs/experiment-notes/`は更新しない。
- 未解決事項: 生成側 / checkerの連携とhardware検証はこの変更の受入証拠に含まれない。
- 関連Issue / PR / commit: Issue #513。

## 2026-09-13: #514 OSC / UDP transport composition

- 日付: 2026-09-13
- 目的: 明示permissionとfreshなP5 allowに限定したgeneric OSC / UDP output経路をruntimeへ接続し、encoded bytes、codec identity、local send resultの証拠levelを区別する。
- 実施内容: `runtime.output.transport_adapter`がrequest、P5 binding、candidate、target、endpoint、revision、lifecycle、operator gateを照合し、version付きpure wire encoderから一つのOSC datagramを生成する。transport configはcodec ID / versionに加えてimmutable settings digestをexpected identityとして固定する。attemptとlocal recordingはtyped encoded bytes、codec identity digest、datagram digestを保持する。sender receiptは`simulated`と`local_socket`を分け、receiver ACKはunavailableのままにする。
- 検証: adapter、lifecycle、output boundary、runtime ownership、import boundary focused testsで73 passed、exit status 0を確認した。P5 core、safety gate、output trace、request schema regressionで117 passed、exit status 0を確認した。fake senderとinjected fake socketだけを使い、DNS resolution、実socket、network、serial、robot outputは実行していない。
- 現在できるようになったこと: 明示的な`transmission_enabled`時だけ、active latest P5 allowとconfig identityが一致したrequestを一度だけgeneric UDP providerへ渡せる。fake acceptanceをlocal socket受理へ読み替えず、attempt / recording bytesとcodec設定identityを照合できる。
- 実験的価値: request / safety bindingとcodec設定、実際のencoded byte列、送信sourceのevidence levelをsoftware回帰で再検証できる。これはreceiver受理やphysical movementの証拠ではない。
- まだ言えないこと: 実DNS / UDP送信、receiver ACK、robot受理、physical movement、実機安全性は未検証である。generic layerにrobot固有joint mapping、unit、calibrationは含めない。
- 判断: explicit transport capabilityとその証拠境界が変わるため当月research logを更新した。実験条件やphysical fixture、観測結果を新規取得していないため`docs/experiment-notes/`は更新しない。
- 未解決事項: receiver ACK correlationとrobot-specific command mappingは別scopeであり、#515 hardware validationや#509 physical evidenceを認定しない。
- 次の作業: exact-head independent reviewとCIはrootが管理する。実送信とhardware validationはこのtaskでは行わない。
- 関連Issue / PR / commit: Issue #514。

## 2026-09-13: #515 FastArm operator gateとrouter observation

- 日付: 2026-09-13
- 目的: FastArmのjoint outputをgeneric transportへ接続する際に、accepted #509 physical-measurement handoff、明示mapping、P5、二つの独立permission、operator gateを同時に要求し、router observationの証拠範囲を限定する。
- 実施内容: `runtime.output.fast_arm_adapter`がprofile/plugin identityとruntime/target identityを分け、`runtime_robot_id == target_robot_id == transport_config.target_robot_id`、accepted evidence / envelope / P2 resolution / collision targetの一致を検証する。requestごとにendpoint / revision / session / cadence identity、P5 candidate、accepted envelope digest、joint別physical-measurement source、`physical_actuation`と`transmission_enabled` permissionも照合する。FastArm codecのversioned mappingはjoint order、per-joint sign、offsetと明示offset unitをdigestへ含み、rad-to-degree pure transformとOSC command semanticsを使う。これらはrouter motor calibrationやshoulder-mount補正を複製しない。実mapping値の選択は#509 / #516 preflightへ残す。generic v2 external-authorization configとone-shot grantを要求し、ACK parserはsource token、target、command、float32値、期限を相関する。simulated observationをreceiver evidenceへ昇格せず、preflight中のoperator stop後には送信しない。
- 検証: FastArm / generic transport focused testsで32 passed、exit status 0を確認した。P5 core、output trace、request schema、import / output boundary、runtime ownership regressionで122 passed、exit status 0を確認した。fake sender / fake socketのみを使い、DNS、実socket、network、serial、robot outputは実行していない。
- 現在できるようになったこと: synthetic typed handoff fixture上で、FastArm mapping、operator gate、二重permission、transport one-shot authorization、pending / correlation / timeout / stopをsoftware-onlyに検証できる。
- 実験的価値: robot-specific joint semanticsをgeneric transportから分離し、send authorizationとrouter-level command observationのcorrelationを再現可能に検証できる。synthetic evidence fixtureは#509の実測定・accepted artifactではない。
- まだ言えないこと: accepted #509 physical-measurement artifact、DNS / UDP実送信、receiver ACK、Pi / robot受理、movement、physical stop、実機安全性は未検証である。`observe_router_datagram`のactual receive socket producerとtimeout tick schedulerも未実装・未検証である。
- 判断: FastArmのphysical-output compositionとoperator gateが追加されたため当月research logを追記した。新しいphysical measurement、experiment condition、実機観測は取得していないため`docs/experiment-notes/`は更新しない。
- 未解決事項: 実際の#509 accepted handoff、専用hardware-safety gate、実機による別途validationが揃うまでFastArm outputを有効化しない。後続#516 preflight / scopeではbounded receive wiring、timeout / disconnect integration、#514 network validationを具体化する。
- 関連Issue / PR / commit: Issue #515、prerequisite Issue #509。

## 2026-09-13: #515 accepted handoff bytesとidentity binding

- 日付: 2026-09-13
- 目的: FastArm output gateがreference / hashだけの主張やP6 software-only dry-run artifactをphysical acceptanceとして扱わず、#509 handoff内容とtyped envelopeの一致を要求する。
- 実施内容: `FastArmPhysicalEvidenceHandoff/v1`のstrict canonical JSON bytesを導入し、`#509`、accepted status、physical-measurement class、schema / acceptance reference、target / profile / model contract、envelope digest、全joint measurement reference、accepted timestampを結んだ。exact byte digestと全fieldをtyped acceptanceへ照合し、envelope model identityもresolved Profileへ一致させる。P6 schema、procedure、dry-run contractは変更しない。
- 検証: FastArm handoffとP6 operator-validation focused regressionで105 passed、exit status 0。P5 / output regressionで184 passed、architecture boundary regressionで28 passed、いずれもexit status 0。P6 software-only artifact rejection、invalid / duplicate / non-canonical bytes、digest / context mismatchを確認した。実DNS / UDP送信、serial、robot output、実機測定は実行していない。
- 現在できるようになったこと: reference / digestだけ、欠落または不正なbytes、P6 software-only artifact、typed dataとhandoff内容の不一致はaccepted FastArm evidenceとしてsessionへ渡せない。
- 実験的価値: accepted handoffのbyte integrity、identity binding、P6 / P5 evidence class境界をsoftware-only回帰で再現可能に検証できる。このbindingはsource authenticity、署名、GitHub Issue state、またはcaller自身が意図的に作る整合文書の真正性を証明しない。
- まだ言えないこと: 実際の#509 accepted physical-measurement artifactはこのsnapshotに存在せず、test handoffはsynthetic fixtureである。hardware safety、actual source authenticity、receiver / robot acceptance、movement、実機安全性は未検証である。
- 判断: accepted physical evidence consumerの研究上のevidence boundaryを変えるためresearch logを追記した。新しいmeasurement、experiment condition、物理観測は取得していないため`docs/experiment-notes/`は更新しない。
- 未解決事項: production useには#509の実accepted handoff bytesと別途hardware-safety gateが必要である。
- 関連Issue / PR / commit: Issue #515、prerequisite Issue #509。

## 2026-09-10: R7-J-P2 resolution text identityのfail-closed補正

- 日付: 2026-09-10
- 目的: P2 resolutionのparity、normalized bound、aggregate、lookup経路で文字列subclassのoverrideがidentity判定を迂回しないことを確認する。
- 実施内容: resolution ownerのcanonical text validatorをbuilt-in `str`境界へ統一し、constructorとdeep validatorでsource / joint / frameなどのtext identityを演算子実行前に検証する。nested DTOからresult inventoryを組み立てる前にも再検証し、authoritative serializationへ不正なidentityが到達しないようにした。
- 検証: 文字列subclassの`strip`、equality、inequality、hashで例外を発生させるfocused regressionを追加し、parity constructor、nested bound validator、result inventory経路を実行して236 passedを確認した。実機、physical numeric measurement、serial、OSC、robot outputは実行していない。
- 現在できるようになったこと: P2のresolution text identityがP1のphysical limit identityと同じbuilt-in `str`境界で検証され、placeholderまたはcustom operatorによるparity / authorityの迂回をfail-closedにできる。
- 実験的価値: software projectionとphysical evidenceのtyped identity境界を、nested / aggregate / public accessorの各段階で再現可能に検証できる。
- まだ言えないこと: authoritative physical numeric evidenceは0件のままである。mechanical stop、cable constraint、self-interference、environment clearance、dynamic feasibility、physical safety、実機動作は未検証である。
- 判断: conversion、unit、frame、provenance、seal、status semanticsは変更せず、experiment conditionやphysical measurementを新規取得していないため`docs/experiment-notes/`は更新しない。
- 未解決事項: P3以降のcollision / trajectory / output gateは別Issueのscopeであり、この修正では実装しない。
- 次の作業: stack全体のcanonical validationと独立reviewを実行する。
- 関連Issue / PR / commit: Issue #504、PR #522。current headとCI stateはGitHub metadataを正とする。

## 2026-09-10: R7-J-P2 reviewer指摘のbounded safety補正

- 日付: 2026-09-10
- 目的: P2 provider、envelope decoder、conversion methodの公開境界で、迂回値やnon-finiteな計算結果が後続のresolutionへ到達しないことを確認する。
- 実施内容: `FastArmResolvedBoundsProvider.bound_for()`で委譲前にresultをdeep revalidateし、`PhysicalSafetyEnvelope.from_json_bytes()`はbuilt-in `bytes`だけを受け付けるようにした。`JointSpaceConversion.source_to_joint()`はoverflowをtyped errorへ閉じ、有限な入力からnon-finiteな射影値を返さない。
- 検証: forged result、nested bound tamper、bytes subclassのBOM / decode override、正負の巨大値とtiny gearを含むfocused regressionを実行して244 passedを確認した。実機、physical numeric measurement、serial、OSC、robot outputは実行していない。
- 現在できるようになったこと: provider accessor、strict JSON bytes boundary、conversion resultの各公開経路で、未検証DTO・bytes override・non-finite projectionをfail-closedに扱える。
- 実験的価値: software projectionの再現可能なbounded validationが、resolver内部だけでなく後続consumerとwire decoderにも適用される。
- まだ言えないこと: authoritative physical numeric evidenceは0件のままである。mechanical stop、cable constraint、self-interference、environment clearance、dynamic feasibility、physical safety、実機動作は未検証である。
- 判断: conversion relation、unit、frame、provenance、seal、status semanticsの契約は変更せず、新しいphysical measurementやexperiment conditionを取得していないため`docs/experiment-notes/`は更新しない。
- 未解決事項: P3以降のcollision / trajectory / output gateは別Issueのscopeであり、この修正では実装しない。
- 次の作業: stack全体のcanonical validationと独立reviewを実行する。
- 関連Issue / PR / commit: Issue #504、PR #522。current headとCI stateはGitHub metadataを正とする。

## 2026-09-10: R7-J-P2 reviewer指摘のstored primitive境界補正

- 目的: `PhysicalSafetyEnvelope.robot_id`のplaceholder identity、aggregate tuple subclass、nested numeric subclassがconstructor後のdeep validation、seal比較、serialization、conversion operationを迂回しないことを確認する。
- 実施内容: envelopeの`robot_id`をconcrete identity validatorへ統一し、保存する`limits`、`bounds`、`parity`、`source_names`、`conversion_relations`、`expected_joint_names`へbuilt-in `tuple` guardを追加した。constructorの入力numeric normalizationは維持し、保存後のnested numeric fieldはbuilt-in `float`だけをdeep validatorで受け付けるようにした。
- 検証: tuple subclassのstateful iteration、float / int subclassのcoercionとconversion sign spoof、robot IDのconstructor / strict JSON decodeをfocused regressionで確認し、263 passedを確認した。実機、physical numeric measurement、serial、OSC、robot outputは実行していない。
- 現在できるようになったこと: stored collectionとnumeric fieldの検証がuser hook実行前に固定され、nested aggregateのidentity / range / serialization mismatchやsign spoofをfail-closedにできる。
- まだ言えないこと: authoritative physical numeric evidenceは0件のままである。mechanical stop、cable constraint、self-interference、environment clearance、dynamic feasibility、physical safety、実機動作は未検証である。
- 判断: constructorの既存入力受入とnormalization、conversion relation、unit、frame、provenance、status semanticsは変更せず、新しいphysical measurementやexperiment conditionを取得していないため`docs/experiment-notes/`は更新しない。
- 未解決事項: P3以降のcollision / trajectory / output gateは別Issueのscopeであり、この修正では実装しない。
- 次の作業: stack全体のcanonical validationと独立reviewを実行する。
- 関連Issue / PR / commit: Issue #504、PR #522。current headとCI stateはGitHub metadataを正とする。

## 2026-09-10: #504/#522 fast_arm physical limit resolution adapter境界分離

- 日付: 2026-09-10
- 目的: fast_arm固有のTOML projectionとprovider assemblyをadapterへ分離し、generic runtimeのphysical limit resolver / DTO / provider / MuJoCo collectorとの責務境界を明確にする。
- 実施内容: `fast_arm_toml_limits_to_physical_limits`と`build_fast_arm_resolved_bounds_provider`を`plugins/robots/fast_arm/adapter/physical_limit_resolution.py`へ移した。adapterは`FastArmJointLimitConfig`と各jointのbuilt-in primitive境界を先に検証し、core constructorのschema、metadata、有限値、canonical joint inventory validationでsnapshotを再構成する。profile joint nameのcanonical順序とprofile boundsの未知keyをfail-closedにし、既存のgeneric resolutionとMuJoCo collectorへvalidated projectionだけを渡す。software-only sourcesのprovisional status、unit、frame、provenance、conversion semantics、public schemaは維持した。
- 検証: physical limit focused regression、fast_arm adapter joint-limit regression、`fast_arm_core_boundary` / `import_boundaries` architecture testsを実行し、203 passed、exit status 0を確認した。`git diff --check`と変更PythonのAST parseもpassした。実機、physical numeric measurement、serial、OSC、robot outputは実行していない。
- 現在できるようになったこと: fast_arm固有入力のtyped projectionとprovider組立をadapter境界で再現可能に検証でき、generic runtimeはrobot-specific config parserをimportせずresolution primitiveを提供できる。invalid config、primitive subclass、nested type、partial / reordered inventory、extra profile keyはresolutionへ到達する前に拒否される。
- 実験的価値: software model / profile / TOMLのprovisional evidenceをphysical authorityへ昇格させず、adapter projectionとgeneric resolutionの研究利用上の責務を独立に再検証できる。
- まだ言えないこと: authoritative physical numeric evidenceは0件のままである。mechanical stop、cable constraint、self-interference、environment clearance、dynamic feasibility、physical safety、実機動作は未検証である。
- 判断: 既存architectureで定めたadapter責務への配置と、不正configを拒否するprojection境界の研究上の再現性を記録するため当月research logを更新した。新しいexperiment condition、model fixture、physical measurementは取得していないため`docs/experiment-notes/`は更新しない。
- 未解決事項: P3以降のcollision / trajectory / output gate、実機のrange測定、外部consumerのstack全体validationは別gate / scopeであり、この修正では実装しない。
- 次の作業: stack全体のcanonical validationと独立reviewを実行する。
- 関連Issue / PR / commit: Issue #504、PR #522。current headとCI stateはGitHub metadataを正とする。

## 2026-09-10: #504/#522 physical limit enum singleton境界の補正

- 日付: 2026-09-10
- 目的: P1 physical limitとP2 resolutionのtyped enum fieldが、値文字列の等値だけでcanonical identityを通過しないことを保証する。
- 実施内容: `EvidenceStatus`、`LimitSpace`、`LimitQuantity`、`ParityStatus`、`LimitResolutionStatus`を共有helperで検証し、constructor / decoderではexactなbuilt-in `str`だけをcanonical memberへ正規化した。stored deep validatorは実member singletonだけを許可し、raw stringと同じenum classを装う偽造memberをseal、authority、serializationへ到達させない。既存のstatus、space、quantity、parity、conversion semanticsは変更していない。
- 検証: root確定の`str.__new__`と`object.__setattr__`による偽造`LimitQuantity`を含むconstructor / deep validator / serialization回帰、および5 enum族のraw string / forged member回帰を実行し、focused safety suiteで295 passed、exit status 0を確認した。実機、physical numeric measurement、serial、OSC、robot outputは実行していない。
- 現在できるようになったこと: constructor入力の文字列正規化を維持しながら、構築後のenum fieldへraw stringまたは偽造memberを注入したDTOをfail-closedに再検証できる。enum値の文字列等値だけでstatusやauthorityを変更する経路は閉じた。
- 実験的価値: software projection、typed physical evidence、parity resultのenum identity境界を、nested validatorと外部sealを含めて再現可能に検証できる。
- まだ言えないこと: authoritative physical numeric evidenceは0件のままである。mechanical stop、cable constraint、self-interference、environment clearance、dynamic feasibility、physical safety、実機動作は未検証である。
- 判断: enum identityのbug fixはresolutionのfail-closed behaviorと研究上の再現性に影響するため当月research logを更新した。新しいphysical measurementやexperiment conditionは取得していないため`docs/experiment-notes/`は更新しない。
- 未解決事項: P3以降のcollision / trajectory / output gate、実機のrange測定、外部consumerのstack全体validationは別gate / scopeであり、この修正では実装しない。
- 次の作業: stack全体のcanonical validationと独立reviewを実行する。
- 関連Issue / PR / commit: Issue #504、PR #522。current headとCI stateはGitHub metadataを正とする。

## 2026-09-13: Issue #414 software-only virtual reaction-force signal

- 日付: 2026-09-13
- 目的: #413のMuJoCo raw measured contact evidenceから、実デバイスへの送信を行わない仮想反力signalを再現可能に導出する。
- 実施内容: source ContactTaskManifest digestに結び付くversioned signal manifestと、frame変換、deadband、moving average、one-pole low-pass、vector rate limit、magnitude clampを持つsoftware-only processorを追加した。raw forceとderived signalを別type / identity / statusにし、target objectのobject_on_tool forceだけをsourceとして記録する。trial変更、no_contact、unavailable、invalid、staleでfilter stateを消去し、raw forceをderived outputで上書きしない。
- 検証: deterministic input fixtureでmanifest digest / strict decode、world-to-tool transform、filter order、clampとraw併記、lifecycle、trial reset、stale、invalid transformを確認した。focused suiteは5 passed。検証用ContactEvidenceはsoftware test fixtureであり、実機やparticipantの測定ではない。
- 現在できるようになったこと: 同じraw evidence stream、signal manifest、trial境界、per-sample transformから同じcanonical signalを再生成し、configurationを含むmanifest digestでfilter / frame policyを識別できる。
- 実験的価値: software-only contact force streamの比較条件とderived outputを、raw measurementを保持したまま再現できる。
- まだ言えないこと: force device選定、device-specific mapping、実機の力覚・安定性・安全性、participant response、robot outputは評価していない。
- 判断: derived contact signalとparameter identityの追加が研究上比較可能な出力を拡張するため当月logを更新した。fixture validationのみで新しいexperiment condition、physical measurement、実行観測は取得していないためdocs/experiment-notes/は更新しない。
- 未解決事項: physical output、device mapping、stability証明とviewer presentationは別scope / gateで扱う。
- 関連Issue: #414

## 2026-09-13: Issue #416 contact logとviewer接続

- 日付: 2026-09-13
- 目的: #413 raw contact evidence、#414 derived virtual reaction-force signal、#415 raw-evidence-only task state / outcomeをversioned logと読み取り専用viewerへ接続する。
- 実施内容: 既存`experiment-motion-log/v1`を維持し、manifest digest、scene、trial identityをsample / summaryへ結び付ける`contact-task-log/v1`とstrict reader / atomic writerを追加した。payload-v0へoptionalな`metadata.contact_task_v1`を接続し、outer payload time / frame indexをcontact sampleと照合する。Product Viewerはcube、contact point / normal、raw evidence、derived force、task outcome、availabilityを読み取り専用で表示し、local contact log / payload-v0 file inputでoffline確認できる。Robot profile、qpos dimension、task outcomeのraw evidence依存は変えていない。
- 検証: Python contact-log / runtime focused suiteは101 passed。viewer parser test 1 passed、TypeScript typecheck / test compile、Vite production buildはexit status 0。visible browser QAは4ケースすべてでcurrent task state / outcome assertions pass、page / console errorsは0、12 HTTP requestsはすべてlocal fulfill。cube / contact付近と+x矢印、stale / reused metadata時に3D overlayとderived force / task state / outcomeが消え、raw historical valueとsynthetic noticeが保持されることを確認した。
- 現在できるようになったこと: versioned contact / force evidenceとtask outcomeのbindingを保ち、offline viewerとpayload metadata経由でcube contact / derived-force presentationを確認できる。outer payload時刻またはframeとの不一致はstale / unavailableとして扱い、古いforce表示を残さない。
- 実験的価値: raw contact evidence、derived signal、task outcomeを区別したうえで、同一manifest / scene / trialへ束縛されたsoftware logとviewer presentationを比較できる。
- まだ言えないこと: MuJoCo scene/reset/extractorを通した#417 end-to-end evidence、物理force、haptic device、participant response、実機出力はこの検証では確認していない。
- 判断: contact log / viewerの追加はsoftware researchで記録・比較できる能力を拡張するため本logを更新した。synthetic fixtureとsoftware test / buildのみであり、新しいexperiment condition、physical measurement、実行観測を取得していないため`docs/experiment-notes/`は更新しない。
- 未解決事項: #417のMuJoCo runtime evidenceはtask gateで確認する。physical outputとdevice mappingは対象外である。
- 次の作業: #417のruntime evidence境界を別途検証する。
- 2026-09-14追記（#536内容監査修正）: headerのTask条件とsummary outcomeの重複条件をPython typed construction / strict decodeとoffline viewerで照合し、同じtrial artifactが矛盾した評価条件を主張することを拒否する。viewerのderived frame / statusを#414のcanonical語彙へ同期し、`device_neutral`は読み取り専用表示に留めてworld矢印へ変換しない。Task式とraw evidenceの所有権は変更しない。条件一致と語彙のfocused regressionを追加した。これは研究artifactの妥当性に関わる修正であり、software validationの完了後も独立内容監査を待つ。新しい観測・experiment evidence・hardware検証は取得していない。
- 関連Issue: #413、#414、#415、#416、#417

## 2026-09-13: Issue #417 deterministic contact E2E smoke

- 日付: 2026-09-13
- 目的: contact scene/resetからMuJoCo solver evidence、derived force、raw-evidence Task、versioned log、同じsnapshotのviewer payloadまでを一つの有限software pathで接続して確認する。
- 実施内容: RobotBundleの一時asset copyに明示software proxyを追加し、Cartesian tool-target点列をJacobian pseudoinverse IKで解いてqposを定め、quasistatic stateを`mj_forward`だけで更新する有限scriptと厳密なversioned summaryを用意した。`mj_step`による時間積分は行わない。実commit指定はscript所在地で解決するGit HEADとの一致とtracked-cleanを生成前に検証し、`test-only-...`は実commit証拠と区別したfixture identityとして扱う。source revision、fixture version、model input digest、proxy identityをmanifestとsummaryに束縛し、raw evidenceとclamp済みderived forceを分離したままTask success、log、viewer payloadへ結んだ。ContactScene全体のqpos列は維持し、resolved profileと実MuJoCo modelのcanonical joint addressを`metadata.contact_scene_robot_qpos_v1`へ同一scene / manifest / frame / time bindingで記録する。
- 検証: deterministic captureを2回比較し、scene reset identity、valid Task sequence、5つのfail-closed negative control、strict log / summary read-back、同じtime / frame / full qposのpayloadをsoftware integration testで確認した。Python contact-log / E2E suiteは16 passed。viewer TypeScript typecheckとtest compileはexit status 0、contact projection assertionsは1 passed、qpos sync suiteは21 passed。これはCartesian-target Jacobian-IKとquasistatic `mj_forward`を使うsoftware fixtureであり、実機・participantの観測ではない。
- 現在できるようになったこと: contact scene/reset、raw contact evidence、derived force、raw-evidence Task、contact log、viewer snapshotを同じmanifest / scene / trialへ束縛して有限・決定的に再検証できる。viewerはversioned mappingを検証してfull scene qposからloaded Robot profileのcanonical joint valuesだけを反映でき、mappingやcontact bindingがstale / mismatchedならfail-closedにする。
- 実験的価値: software上のcontact evidenceとforce displayを、raw Task判定と明示revision / model / proxy identityを保って反復比較できる。browser上の最終scene-application確認はrootのoffline viewer QA gateである。
- まだ言えないこと: Cartesian-target Jacobian-IKとquasistatic `mj_forward`だけのfixtureはdynamic stability、trajectory safety、physical force、haptic device、participant response、実機動作を示さない。physical evidenceは取得していない。browser-visible QA、exact-head CI、独立reviewはまだ完了扱いにしない。
- 判断: runtime integrationにより再現できるresearch capabilityと主張境界が変わるため当月logを更新した。今回のfixture、integration test、通常smokeはユーザーtaskでsoftware-only validationに分類され、pilot conditionまたはphysical observationを取得していないため`docs/experiment-notes/`は更新しない。
- 未解決事項: exact-head CIとoffline Product Viewer visible QAはcommit後のroot gateであり、このlocal test結果から代替しない。
- 次の作業: commit後のexact-head validationとvisible viewer QAを完了し、draft lifecycleを維持する。
- 関連Issue: #413、#414、#415、#416、#417

## 2026-09-14: #513の実評価candidateとoutput requestのbinding修正

- 目的: 安全なcandidate Aのevidenceをrequest B用のIDへ付け替えてallowできる不整合を解消する。
- 実施内容: P3のMuJoCo観測producerとP4 evaluatorが実評価qpos / qvel / sample時刻を保持し、runtimeがjoint targetと両checkerを照合する。
- 研究上の意味: software-only output候補と実際に評価したconfigurationの対応を追跡できる。callerのID申告だけを対応の証拠にしない。
- 制約: endpoint velocityやresolverのないtrajectoryはnon-sendable。configuration評価は移動軌道・実機安全性の証明ではない。physical numeric evidence、formula、wire semanticsは変更しない。
- 検証: public APIによるA評価→B request→ID付替えの拒否、対応する候補のallow、checker間不一致のfail-closedを対象とする。unit / regressionはexperiment evidenceではない。
- 次の作業: #534 / #535 / #537はDraft・未mergeのまま独立内容監査を受ける。#509 / #516、実socket / OSC、hardwareは未実施。

- focused review反映: 数値qposが同じ別endpointへの付替えも拒否するため、runtime設定のendpoint / joint順序routeを実観測・実評価originへ保持する。

## 2026-09-20: #513 candidate qposとauthoritative position boundの照合

- 日付: 2026-09-20
- 目的: 実評価candidateとrequestのidentity bindingが成立していても、candidate qpos自体がauthoritative physical position bound外ならsendableにならないことを保証する。
- 実施内容: P2 `LimitResolutionResult`へ実評価joint順序 / qposをauthoritative resolved boundと照合するcanonical helperを追加し、P5はP3/P4の実評価candidateが一致して全componentがallow候補になった時だけこのhelperを呼ぶ。1 jointでも`lower_rad <= qpos <= upper_rad`を満たさなければ`limit_candidate_out_of_bounds`としてrejectする。P2のconversion / parity / authority formulaやphysical limit数値は変更しない。
- 検証: authoritative範囲内、lower / upper境界、上限超過、下限未満、joint order / dimension mismatch、candidate IDをrequestへ合わせた範囲外candidateをsoftware-only regressionで確認する。P3/P4のcandidate bindingとresolverのないcommand semanticsのfail-closed挙動は維持する。
- 現在できるようになったこと: output候補はrequest、P3/P4実評価configuration、endpoint / joint routeだけでなく、P2のauthoritative physical position rangeにも同じcandidate qposで結び付けて判定できる。
- 実験的価値: 実機bring-up前のsoftware gateで、authoritative rangeが存在するだけで範囲外commandをallowする不整合を除去し、physical output候補の再現可能なreject理由を固定できる。
- まだ言えないこと: configuration-only判定はcurrent→targetのtrajectory safety、actual mapping sign / offset、physical stop、robot movementを証明しない。physical measurement、socket / OSC、serial、robot output、#509 / #516 hardware validationは実行していない。
- 判断: physical output候補の成立条件とresearch上のsafety boundaryが変わるため当月research logを更新する。unit / regression / CIはexperiment evidenceではないため`docs/experiment-notes/`は更新しない。
- 未解決事項: actual FastArm mapping / deployed configと#509 accepted evidenceのfreeze、bounded transition / receiver integrationはhardware開始前の#509 / #516 handoffで扱う。
- 次の作業: #534のfocused / regression / architecture validation後、#535 / #537をmechanical restackし、clean-room内容監査を受ける。
- 関連Issue / PR: Issue #513、PR #534。current headとCI stateはGitHub metadataを正とする。

## 2026-09-20 — 実機前SIL設計と連続Selfrionette経路（#539 / #540）

ユーザー方針により、実機前に信号エミュレーションのsoftware gateを設けた。
調査で共通step loopのLOADCELL_SOURCE未対応と、専用runnerのper-frame model再生成を確認した。
連続経路では一つのmodel、同stepのmeasured-tip context、既存model-aligned local solverを使用する。
7ch Mappingは位置増分/sampleの意味を維持し、Gamepad velocityの意味へ変更していない。
synthetic mappingによるunit/integration検証のみで、実機calibrationや実験結果ではない。
bounded acquisition、protocol peer、contact統合、final completionは後続#541-#543に残る。
hardware/serial/Arduino/robot-control network/OSC実送信/physical actuationなし。

## 2026-09-20 — 連続入力のroute/context整合（#540 / PR #544）

#545 / ADR 0005（PR #546）の判断に従い、入力adapterではなくresolved typed routeへ
local generatorとdelta/velocity変換を集約した。同じsignal/configでstep loopとrun_onceの
backend requestを照合し、旧反例の入口依存を解消する。固定Mapping parameterから
観測contextを分け、continuous callerにcurrent-tip placeholderを要求しない。
既存software上限は緩めず、Mapping要求・制約後delta・指令・MuJoCo観測を分離する。
検証は合成信号のsoftware回帰であり、実機・実験結果ではない。#541-#543の取得/通信/接触
統合とphysical validationは残る。新しい汎用pipelineやregistryは導入していない。

## 2026-09-20 — merge前監査でのmanaged health整合修正（#540）

正常入力の入口一致だけでは不十分であり、stale healthを持つ同じ7ch入力がstep loopではhold、
run_onceでは移動する反例を確認した。既存healthの照合を共有し、単発実行でも非activeを保持する。
取得処理自体や時間閾値は変更せず、#541のbounded acquisitionとは分離した。
synthetic fault injectionによるsoftware検証であり、実機停止の証拠ではない。

## 2026-09-20 — 実機前SILの全体整合調査（#545）

既存capやmetadata利用それ自体を欠陥とした前回self監査の断定を訂正した。
同じdelta routeがstep loopとrun_onceで異なる指令を作る反例を確認し、route/context境界を修正対象にした。
構造棚卸しは全tracked file、意味確認は主要実行境界に限定し、全コードの無欠陥を主張しない。
詳細は`docs/reports/audits/r7-l-consistency-baseline-2026-09-20.md`。実装・実測・実験結果とは区別する。
#540-#543とphysical validationは未完であり、hardware/serial/OSC実送信なし。

## 2026-09-20 — 有限Selfrionette取得（#541）

診断line探索と無信号readを有限化し、取得失敗をnormal sampleと区別した。
start直後は未受信のinactiveとし、成功vector（zero含む）のhost受信時刻でhealthを評価する。
device timestampは保持し、決定的fixtureの静的clockと実時間観測を区別する。
既存runtimeの取得例外による終了を使い、過去値再送、自動再接続、新しいpolling frameworkは追加しない。
software-onlyのfake serial/clock・MuJoCoでの検証であり、実機のstartup/cadence/noiseを検証した結果ではない。
ADR 0006へ判断を記録した。#542-#543とphysical validationは引き続き別gateである。

## 2026-09-20 — no-I/O FastArm protocol境界（#542）

#541 / PR #547のmerge後、pure joint変換からOSC bytes、in-memory peer、疑似router応答までを接続した。
実機sessionのpermissionは変更せず、parser/correlation/expiryの判定だけを共有する。
明示したsynthetic mapping、固定byte oracle、誤応答・timeout・切断で検証する。
これはsoftware protocolの成立確認であり、実機のaxis/zero校正、router配備互換、ACK、運動、停止の実証ではない。
詳細判断はADR 0007、current contractはphysical-output。Input Source/Task/contact/artifact統合は#543に残る。

## 2026-09-20 — P4信号/contact統合（#543）

P3 PR #548のmerge後、production入力から単一実行scene・raw接触Task・no-I/O wireへの結線を追加。
Robot4軸とcube freejointを名前/addressで分離し、cube速度を保持する。direct-qpos semanticsを維持する。
合成Gamepad/7ch入力で同じ条件のcontact成功を観測し、zero入力を対照とした。
実行で既存Taskのnormalized dot 1.0000000000000002によるoutcome拒否を確認し、producer側の数ulp補正を追加。
実測や性能比較ではなく、software integration smoke。実機根拠は未取得、P5はnon-allow、output disabled。
設計・却下案はADR 0008。CLIはlocal fixture/outputのみで実機・serial/network I/Oを行わない。

P4自己監査の反例: 制約外reset、最終payload profile、表現不能deadline、再hashした元入力/healthを検証し、
既存guard・profile/metadata照合・production Source再生により修正した。hashのみを正しさの証拠とはしない。

## 2026-09-20 — R7-Lマージ後の受入境界修正（#550）

PR #549後の点検で、評価器の追加指定の無視と、traceの診断・終了情報の整合検証不足を再現した。
現行runnerはcontact_outcome/v1を1件だけ事前受理し、診断は既存source状態とguard、終了は最終Task advanceと実行件数から照合する。
正常fixtureとphysics条件を変えず、架空の実機証拠や新しいphysics engineは追加しない。ADR 0009に判断を記録する。
これは合成条件のunit/integration検証であり、実測experiment resultではない。実機用sessionへのsoftware結線は#551へ分離して残す。

## 2026-09-20: physical sessionへの入力接続（#551）

PR #552 merge後のmainから、既存入力plan、P5判定、physical output session、受信driverを有限ownerへ結ぶ。
元source時刻とhost指令時刻を分離し、pending時の入力消費・次commandを止める。
試験は既存test-only acceptanceとfake senderのみであり、実機の実測・運動・停止の証拠ではない。
local simulationはcommand生成用で、physical safety inputの未取得値を測定済みとして埋めない。
#516へ残すactual receiver/bind、配備/校正、physical stop確認はこの接続の合格と区別する。

#551の初期固定SHA監査では、prepare中の375 ms遅延で入力stale後にfake dispatchできる反例を再現した。
既存sessionへ拒否専用のprepare後callbackを追加し、入力freshnessと終了状態を再確認する。既存P5/permission/grantの条件は緩めない。
最終内容監査では、viewerのsource subtypeがread後またはprepare中に切り替わってもfreshnessだけでは検出できるとは限らない点を追加で確認した。
frame/typed healthのsubtype整合と、同一commandについてprepare後までsubtypeが不変であることを検査し、identity変更時は送信前にfail-closedとする。

## 2026-09-21: 起動設定の明示化（#554 / #555）

software-onlyのRobot/Input/Mappingと有限実行条件をversion付きJSONとして検査・表示できる入口を追加する。
既存resolverへ委譲し、設定digest、展開済みdefault、software revisionを区別する。
これは起動条件の取り違えを減らす運用・再現性の改善であり、新しい制御方式や実測結果ではない。
実機permission、accepted evidence、serial/OSCは対象外。統一起動と操作画面は別の変更として追跡する。

## 2026-09-21: 一つのprofileからのWeb/backend起動（#557）

既存publisherへ写像設定・route・robot version・入力providerを渡す統一起動を追加する。
実行条件の検査、有限runtime、片側終了時のcleanup、Windows venv redirectorとのPID差を扱う。
これは操作手順と条件の取り違えを減らす改善であり、実機作動や物理停止は確認していない。

## 2026-09-21: 操作画面の情報階層（#559）

3D・入力・受信鮮度を同時に表示し、描画readyを接続や実機安全性と分離した。
取得停止と詳細診断、カメラframingは表示/操作導線の変更で、physics/Mapping/Task判定を変更しない。
参加者実験ではUI条件をrevisionにより固定する必要がある。今回はsoftware browser smokeのみである。

## 2026-09-21: 関節・入力計器と診断固定（#561）

- 目的: 高速な数値列を読む作業を減らし、3D操作中に姿勢と入力信号を確認できるようにする。
- 実施内容: loaded MuJoCoのjoint name/type/addressを使う角度指標、Gamepad XY/軸/bar、Keyboard状態、Selfrionette 7ch表示をworkbenchへ追加した。一般のqposを全て角度と扱わない。
- raw入力はMapping後のcompatibility値とは別に、取得frameからoptionalな`input_signal_v1`へ投影し、source/sample schemaとsimulation frame/timeを検証する。source時計とhost/simulation時計を混同しない。
- 表示を最大20 Hz程度、数値を4 Hz程度へ分け、重要状態変化は待たずに反映する。診断snapshotは詳細欄だけを固定し、3D、入力、安全判定、backend周期、実験記録を止めない。
- 検証: Python全体2810 passed / 12 skipped、viewer test/typecheck/build成功。Windows Chromiumのlive操作、desktop/narrow layout、診断固定中のlive frame進行を確認した。詳細な故障注入とfixed-head確認は対応PRへ記録する。
- 研究上の意味: 表示・操作条件が変わるため、比較実験ではviewer revisionと表示仕様を固定する。表示改善だけから制御性能や入力装置の優越性は結論しない。
- 未確認: 実機可動域、安全帯、力単位・指対応の校正、実機停止、参加者による可読性評価。これらをUIの色やメーターから推定しない。
- Experiment evidence impact: software-only test/browser smokeであり、実機・参加者実験の記録は追加しない。
- 関連: 親#554、#555/#557/#559の起動・workbench工程。今回の作業ではmergeを行わない。

## 2026-09-21: UX受入監査で表示証拠の境界を補強

#554の受入監査で、詳細診断のGamepad不正値の整形と、sample schema不明時に上流の
`input_signal_v1`が残る反例を再現した。#561 / PR #562で、軸index・不明buttonの保持と
runtime自身が生成したraw projectionだけを公開する処理へ修正し、回帰試験を追加した。
表示証拠の誤解を防ぐ変更であり、制御値・物理安全性・実機検証の成立範囲は変えない。
独立した新規モデルセッションの静的監査と、実装側の再現・実行検証は別の証拠として記録する。

## 2026-09-25: Gamepadの軸対応を明示して比較条件へ保存する

- 目的: 利用者の方向不一致報告を、raw入力・要求座標・画面投影に分けて扱う（#563）。
- 実施内容: Mappingへoptionalな軸選択と符号設定を追加し、startup前の厳格検証・copy/freeze、active時の軸欠落拒否、適用設定のdiagnosticsを追加。
- 実装: `sim-gamepad-world-xy`は左stick右/上→world +X/+Y、右stick上→world +Z。既存`sim-gamepad`、v1の省略設定、二段deadzone、Zボタン補助、速度scale、world/tool意味を保持する。
- 検証: 新規の設定・source/Mapping統合・launch profile・MuJoCo step回帰を追加。実行結果と対象SHAはPRに記録する。通常のsoftware testであり参加者実験ではない。
- 現在できること: 入力座標対応を明示設定し、backendの同じresolverから再現できる。
- まだ言えないこと: 利用者の実Gamepadとcameraの再現条件は未取得。現物での左右反転解消、任意viewへの直感性、双腕対応、UI/実機動作の受入完了ではない。
- 判断: 論文repository PR #26のマージ後に開始したsimulation-only修正。serial/OSC/実機gateは変更しない。
- 次の作業: #563の実操作確認、#564の複数視点、#565/#486のGUI再試行・タスク選択と対象物生成。後者を本変更で実装したとはしない。

## 2026-09-25: 1スティックXYZ操作を双腕接続より先に実装する

- 目的: 利用者の了承に従い、双腕向け入力をタスクGUIより優先する（#567、前提PR #566）。
- 実施内容: 左右独立XY/XZと中立確認、実行session隔離、片側選択profile、backend由来mode表示。
- 取得: 新方式のみ中立heartbeatと取得session IDを使い、待機時の誤った再arming要求・再開時の系列混同を避ける。旧設定は維持。
- 検証: 設定・状態機械・Source・片腕MuJoCo・viewer回帰を実施し、最終件数とheadはPRへ記録する。通常testを研究実験には数えない。
- 現在できること: 片腕を左stick＋左肩、または右stick＋右肩だけでXYZ操作し、両側の要求を独立表示する。
- まだ言えないこと: 双腕物理モデル、三軸同時独立入力、人の操作受入、実機成立、優越性は未確認。
- 次の作業: 同じ左右入力を双腕Robotへ接続し、複数視点と再試行、ばね・対象物GUIへ進む。実機受入待ちにしない。

## 2026-09-25: 双腕化の共通assemblyと出力対応（#569）

- 目的: 利用者の「逆腕は左右反転」「OSC等でも片側未実装を残さず全体最適」という依頼に対応。
- 実施内容: core原本から1/2腕を生成し、鏡映した位置・回転軸・慣性・meshを同じ名前規約で扱う。adapterは左右の名前付き指令を個別targetと既存OSC要求へ投影する。
- 検証: 合成配置でのFK/Jacobian/慣性/重力、名前address、OSC byte/誤応答/timeoutをsoftware testへ追加。実行結果はPRと実装報告へ分離する。
- 現在できること: 原型片腕・鏡映片腕・同一world双腕の組立API、左右同じ出力変換。旧single-arm profileや入力PR #568は変更しない。
- 実験的価値: 単腕/双腕・scene物体・送信先を別々の配列位置で管理せず、一つのjoint/arm対応から追跡できる。
- まだ言えないこと: 双腕GUI操作、接触用衝突geometry、二台Selfrionette、連成物理送信の完成。元meshは衝突無効であり、接触研究には追加整備が必要。
- 判断: 実装単位を分けても、以後の入力/状態/安全/出力/記録は両側に同じ完了条件を課す。鏡映から実機の符号・zero・限界を推測しない。
- 次の作業: multiple-endpoint provider/routeと単一snapshot共同更新、全scene feasibility、左右outputの連成監督を順に接続する。
- 関連: Issue #569、入力PR #568、GUI #565、複数視点 #564。[assembly契約](../../docs/contracts/fast-arm-assembly.md)。

- 文書整合: 実装roadmapに残った実機優先・装置比較対象外の旧前提を、利用者追認済みRM004へ合わせた。実機用gateはオプション内の順序として維持し、来歴をADR 0011へ保存した。

## 2026-09-26: 両腕の共同runtimeと出力監督（#571）

- 目的: #570のassemblyと左右入力を接続し、片側だけ動作を継続する経路を防ぐ。
- 実施: typed複数手先、同一snapshotでの各腕DLS/既存限界、全候補の検査と一括反映、中立・鮮度・epoch・全体faultを実装。
- 出力: 各側の既存P5/二重permission/codecを維持してprepareとdispatchを分離。全側準備前の送信を防ぎ、部分失敗/timeoutで両側停止要求を試みる。
- 実行意味: direct-qposの運動学診断。実測の動力学・接触力・実機安全性ではない。host受信/要求時刻とsimulation時間を分離。
- 検証: 単腕original/mirrored/双腕6方向、片側候補失敗の非反映、入力期限切れ/再開、全側prepare/部分送信/異常応答/停止失敗を合成no-I/Oで試す。最終結果はPRのheadと照合する。
- まだ言えないこと: GUIからの双腕操作、接触研究、receiver watchdog/独立非常停止、実機停止完了。未確認条件をallowへ変換しない。
- 次: scene表示/複数視点、二台入力、task/ばね接触と衝突・動力学を別の受入単位で接続する。既存実機gateは保持。
- 関連: Issue #571、[共同実行契約](../../docs/contracts/coordinated-arm-runtime.md)。

## 2026-09-26: 肩mount 30 degree開きの責務監査とモデル反映（#575）

- 目的: 左右FastArmの取付板が正面視で `/ \` となる30 degree開きを、simulationとphysical outputのどの層が所有するか確定する。
- 監査: `fast-arm-router` revision `8d8c3a6`は4DOFを `(j0-j1, j0+j1, j2, j3)` へ差動変換するが、30 degreeの3D mount transformやtarget別mount offsetを持たない。
- 判断: 30 degreeはmotor zero / joint offsetではなく固定base geometryである。routerへ補償を追加せず、FastArm assemblyでmirror後に左X `+30 degree`、右X `-30 degree`のmount rotationを適用する。
- 実施: 双腕diagnostic fixtureと関連MuJoCo/runtime regressionを同じmount quaternionへ更新し、physical-output / assembly contractの責務記述を修正する。
- 検証: assembly core / MuJoCo integration / coordinated runtimeのfocused suiteは70 passed。compileall成功、strict Markdownは259 files / 80 SoT topics / errors 0（既存warning 6）、`git diff --check`成功。
- 保持: `position_m=(0, +/-0.4, 0)`は合成配置のままで、実機mount間隔・高さを意味しない。motor sign、encoder zero、実機limitも30 degreeから推論しない。
- 実験的価値: world上のtip pose、Jacobian、workspaceを、提示された肩取付姿勢を含むgeometryで評価できるようにする。
- まだ言えないこと: 実機との位置一致、motor校正、collision/contact、physical safety、実機停止は未検証である。
- Experiment evidence impact: software/model contract変更とno-I/O validationであり、participant / hardware experiment evidenceは追加しない。
