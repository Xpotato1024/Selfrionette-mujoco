---
status: historical
owner: research
last_verified: 2026-07-29
canonical_for: []
related:
  - research/README.md
  - https://github.com/Xpotato1024/Selfrionette-mujoco/issues/398
  - https://github.com/Xpotato1024/Selfrionette-mujoco/issues/399
  - https://github.com/Xpotato1024/Selfrionette-mujoco/pull/400
  - https://github.com/Xpotato1024/Selfrionette-mujoco/pull/401
---

# 2026年7月 研究・実装ログ

## 2026-07-16: 月次ログ運用の開始

- 日付: 2026-07-16
- 目的: 実装事実、実験的価値、未検証事項を混同せず、月単位で追跡できる記録基盤を導入する。
- 実施内容: `research/README.md`に記録対象とentry templateを定義し、当月logを作成した。
- 検証: 文書構造、relative link、UTF-8 without BOM、LF、mojibake、inventory分類をdocumentation validationの対象にした。
- 現在できるようになったこと: 今後の変更について、実装結果と研究上の判断を同じentry内で分離して追記できる。
- 実験的価値: 実験可能性や主張範囲が変化した時点を、implementation evidenceと混同せず追跡できる。
- まだ言えないこと: このentryは運用開始の事実だけを記録する。過去の実験結果、性能改善、再現性、研究仮説の妥当性は示していない。
- 判断: PR本文の詳細validation evidence、`docs/experiment-notes/`の条件・観測結果、research logの研究判断を分離する。
- 未解決事項: 後続変更でentry更新要否の運用実績を確認する。
- 次の作業: #399で文書配置とcurrent human-facing docsの日本語方針をinventoryに従って移行する。
- 関連Issue / PR / commit: Issue #398、Draft PR #400、traceability更新前のstable content commit `37f02ac0f6f37148aa681da6fef6e6f386f39956`。merge commitは未作成であり、current PR headはGitHub PR metadataを正とする。

## 2026-07-16: 文書SoTと現役文書のmigration

- 日付: 2026-07-16
- 目的: current specとhistorical evidenceを分離し、人間向けのcurrent canonical / reusable operationsを日本語から辿れる状態にする。
- 実施内容: #398 inventoryに従ってevidence / historical文書を責務別directoryへ移し、Source of Truth Mapを縮約し、active documentを日本語化した。
- 検証: rename追跡、Markdown link、SoT target / duplication、UTF-8 without BOM、LF、mojibake、front matter、inventory disposition、architecture testsを確認した。詳細な件数とcommand結果は#399 PR evidenceへ記録する。
- 現在できるようになったこと: current architecture、contract、evaluation、反復手順を短い日本語mapから辿り、audit / implementation / inventoryを別入口から参照できる。
- 実験的価値: 実装済み事実と過去の成立証拠を混同せず、今後の実験判断が参照するcurrent contractを特定しやすくなった。
- まだ言えないこと: 文書migrationはruntime behavior、性能、実験結果、研究仮説の妥当性を変更または実証しない。
- 判断: historical / evidence本文は翻訳・現行化せず、current factだけをcanonicalへ統合する。
- 未解決事項: repository-wideの非current supporting / legacy英語負債とhistorical local pathは、必要性が生じたscopeで段階的に扱う。
- 次の作業: #399のhuman review後、mainへ統合するかを明示判断する。
- 関連Issue / PR / commit: #399 / main-base Draft PR #401 / latest mainへのrebase後accepted audit content commit `7561c5e29c4cba0e7221114b7cfafd0459951374`。このtraceability記録は同commitの後続であり、current PR headはGitHub PR metadataを正とする。#399のmerge commitは未作成。

## 2026-07-16: #398 文書governanceのmain統合

- 日付: 2026-07-16
- 目的: #398 / PR #400のfinal merge evidenceを、未作成だったmerge commitの推測ではなくGitHub上の実績として追記する。
- 実施内容: Draft PR #400をReady化し、expected head `f64f6d4c88286e765c748fd9068721946e15f505`を固定してsquash mergeした。mainのmerge commitは`3ce7f30942ec66568b1d602dca0576380ac70fb2`で、Issue #398は`completed`として自動closeされた。
- 検証: GitHub Actions run `29481177492`の`python-validation` / `viewer-validation`成功、merge commitとlatest mainの一致、#398の`CLOSED / COMPLETED`を確認した。
- 現在できるようになったこと: #399固有commitを、#400差分を重複させずlatest mainへ積み直せる。
- 実験的価値: 文書governanceとmigrationを独立したmerge boundaryで追跡できる。
- まだ言えないこと: このmergeはruntime behavior、性能、実験結果、研究仮説を変更または実証しない。
- 判断: #401はbase変更だけで済ませず、#399固有commitをlatest mainへrebaseし、main-baseのvalidationとCIを再実行する。
- 未解決事項: #401のmain-base technical merge-ready確認とhuman review。
- 次の作業: #401のbaseを`main`へ変更し、actual diff、rename、tests、CI、head一致を再検証する。
- 関連Issue / PR / commit: Issue #398 / PR #400 / squash merge `3ce7f30942ec66568b1d602dca0576380ac70fb2`。#400 source branchは保持している。

## 2026-07-17: composable experiment plugin foundation

- 日付: 2026-07-17
- 目的: robot固有runtimeへscene、mapping、task、evaluation責務を集中させず、R7-G / R7-Hが共通の明示composition boundaryから実装開始できるようにする。
- 実施内容: 既存Robot Profile / Runtime Plugin上にtyped capability providerを束ねるRobot Bundleを追加し、Environment、Control Mapping、Task、Evaluationのversioned contract、known-ID registry、canonical evidence、startup readinessを実装した。review監査に基づき、parameter ownerをaxis / ID / versionへ拡張し、evidence producer binding、exact version compatibility、object kind / frame / unitを含むtyped semantic role requirement、evaluator result identity / provenance検証、immutable capability contract mappingを追加した。fast_arm production pathは既存pluginへ委譲するbundleへ接続した。
- 検証: generic non-fast_armのvalid composition、同一raw IDを使う異軸parameterの分離、unknown / duplicate / version mismatch、capability / typed role / evidence mismatch、ambiguous provider / producer、evaluator identity / provenance mismatch、unsupported capabilityのfail-closed testと、fast_arm既存profile/plugin/home contractの回帰確認を行う。詳細な最終test matrixはIssue #421のDraft PRへ記録する。
- 現在できるようになったこと: R7-G-P1 / #405は5軸のplugin selection、axis-scoped parameter、version compatibility、evidence producer bindingをmanifest / freeze identityへ固定でき、R7-H-P1 / #411はtyped semantic role requirement、generic environment/task/evaluator、optional contact-evidence extension pointを前提に設計できる。
- 実験的価値: robot、mapping、task、evaluatorを独立条件として固定し、canonical evidenceからmetricを再導出するための再現可能性boundaryが成立した。
- まだ言えないこと: R7-G runner/pilot、metric妥当性、R7-H cube/contact physics、virtual reaction force、physical safety、participant結果は実装・測定・証明していない。
- 判断: requested / resolved / predicted / measured / unavailable / invalidを分離し、不足capability/evidenceへzero、empty、no-opを適用しない。viewerはrendering-onlyのままとする。
- 未解決事項: #405でevaluation manifestのcanonical serialization/freeze identityを、#411でcube/object parameterとcontact evidence identityを具体化する必要がある。
- 次の作業: Issue dependency順に#405を実装し、その基盤とR7-G completion条件を満たした後に#411へ進む。
- 関連Issue / PR / commit: Issue #421、blocked consumer #405 / #411。本entry時点ではDraft PRとcommitは作業中であり、final identityはGitHub PR metadataを正とする。

## 2026-07-17: #405 evaluation manifest readiness freeze

- 日付: 2026-07-17
- 目的: world / tool control-frame比較を実行前に再構築可能なcondition identityへ固定し、runnerへ渡す前のsoftware-only readiness boundaryを成立させる。
- 実施内容: `evaluation-manifest/v1`のimmutable typed manifest、strict canonical UTF-8 JSON、SHA-256 digest、resolved capability / semantic role / evidence producerを含む`FreezeRecord`、world/tool condition-pair invariant、#421の5軸`compose_experiment()`を使う`EvaluationReadiness`を追加した。#423のpackage migrationではlogical identityを維持することをcanonical contractへ記録した。
- 検証: canonical round trip、field order independence、unknown / missing / duplicate、non-finite、bool-as-number、pose / quaternion、duplicate owner / evaluator、pair invariant、mapping/frame、capability / role / evidence、neutral home、profile / model identity、freeze mutation、generic non-fast_arm、fast_arm readiness regressionをtestで確認した。
- 現在できるようになったこと: 実行開始前にrequested manifest identityとstartup-resolved identityを同じfreeze recordへ固定し、conditionごとのreadiness failureをpartial runner objectなしで拒否できる。
- 実験的価値: experiment condition、initial state、target geometry、input / motion setting、camera / feedback identity、plugin evidence boundaryをdata collection前に同一bytes / digestとして比較可能にできる。
- まだ言えないこと: runner、fixture playback、physics execution、measured reachability、measured tip outcome、task success、metric妥当性、participant結果、hardware safetyは実装・測定・証明していない。
- 判断: #405の`READY`はsoftware configuration freezeだけを意味し、world/toolの物理的同等性や性能差を意味しない。experiment conditionと観測結果は取得していないため`docs/experiment-notes/`は更新しない。
- 未解決事項: #406でこのpublic readiness APIをrunnerへ接続し、後続のlog / outcome lifecycleを別boundaryとして実装する必要がある。
- 次の作業: #423のpackage / import-direction migrationはfreeze logical identityを維持し、#406はready resultを受け取ってからrunner設計を進める。
- 関連Issue / PR / commit: Issue #405、parent #404、follow-up #423、next runner #406。Draft PRと最終commit identityはGitHub metadataで確定する。

## 2026-07-17: #405 audit correction

- 日付: 2026-07-17
- 目的: 監査指摘に対し、manifest parameterのrecursive immutability、actual software execution identity、mapping comparison family、canonical initial-state contractをreadiness / freeze boundaryへ追加する。
- 実施内容: `PluginParameters`はrecursive canonical JSON valueをdetach / freezeし、`SoftwareExecutionIdentity`はexplicit revision schemeとmanifest値のexact matchを要求する。world/tool mappingはversioned family / semantics identityとframeを解決し、fast_armは既存home keyframe由来の`FAST_ARM_INITIAL_STATE_CONTRACT`、generic fixtureはtyped provider contractを使う。target tolerance、dwell、cadenceのcross-field invariantもfail-closedにした。
- 検証: 外部mutable input、nested value、actual repository / revision mismatch、family / frame mismatch、strategy semantic identity mismatch、initial qpos / tip / orientation / keyframe / contract mismatch、freeze identity change、cross-field numeric rejectionを追加testで確認した。
- 現在できるようになったこと: 同じsemantic contentのmanifestを外部mutationから独立したcanonical bytes / digest / freeze identityへ固定し、requested identityとactual / resolved identityの不一致をrunner開始前に拒否できる。
- まだ言えないこと: runner、fixture playback、physics execution、experiment log、outcome / metric、measured reachability、participant結果、hardware safetyは実装・測定・証明していない。
- 判断: 今回はconfiguration identityとsoftware readinessの能力だけを更新した。実験条件や観測結果は取得していないため、`docs/experiment-notes/`は更新しない。
- 次の作業: #423はlogical identityを維持したpackage migration、#406は`SoftwareExecutionIdentity`を渡してready result / freeze identityを受け取るrunner接続を行う。

## 2026-07-17: #423 fast_arm plugin boundary normalization

- 日付: 2026-07-17
- 目的: #406がgeneric runtimeからfast_arm具体moduleを組み立てず、明示catalogとtyped providerからexperiment runner compositionを開始できるようにする。
- 実施内容: fast_armのProfile、Runtime Plugin、feasibility、canonical initial state、Robot Bundle assemblyを`plugins/robots/fast_arm/`へ移し、`plugins/catalog.py`を唯一のproduction concrete registration SoTにした。generic provider adapterとregistry / consistency primitiveをcoreへ分離し、旧pathは同一objectのcompatibility facadeにした。
- 検証: 旧 / 新import object identity、catalog resolver収束、generic non-fast_arm composition、fast_arm conformance / pipeline、#405の固定test revisionによるcanonical manifest bytes / resolved identity material / freeze identity一致、dependency boundary、full Python suiteを確認する。
- 現在できるようになったこと: #406はcatalogでBundleをresolveし、assembly時に必要なtyped providerだけを受け取れる。Profile / Runtime Plugin / Bundleのregistration driftやgeneric coreからconcrete catalogへの逆依存をarchitecture testで拒否できる。
- 実験的価値: package locationをlogical experiment identityから分離したまま、runnerが使用するrobot composition provenanceを単一catalogへ固定できる。
- まだ言えないこと: runner、fixture playback、physics execution、contact evidence、metric、world / tool性能差、participant結果、hardware safetyは実装・測定・証明していない。
- 判断: Bundleはassembly boundaryでありruntime service locatorではない。compatibility facadeは新規consumer APIではなく、package移動だけではversioned identityを上げない。
- 未解決事項: #406でready result / freeze identityを実行lifecycleへ接続し、runner record / outcome boundaryを実装する必要がある。
- 次の作業: #406は`selfrionette.plugins.catalog`、resolved experiment composition、typed providerを使用し、`plugins.robots.fast_arm`を直接importしない。
- 関連Issue / PR / commit: Issue #423、blocked successor #406。baseline mainは`e0311688f8d9738689434a82895616c42e965c0f`。Draft PRとfinal commitはGitHub metadataを正とする。

## 2026-07-17: #423 audit hardening

- 日付: 2026-07-17
- 目的: #423のpublic compatibility、typed provider injection、identity preservation、production import boundaryを監査可能な機械検査へ固定する。
- 実施内容: `selfrionette.runtime`のpublic exportを明示owner mappingへ変更し、step loop planとoffline smokeからbroad Runtime Plugin利用を除去した。baseline mainを実行して得たmanifest / resolved identity / freeze digestをgoldenに固定し、production concrete importをAST allowlistで検査する。
- 検証: 全root public exportのowner object identity、catalog-free generic import、typed providerによるendpoint pose / motion / qpos guard、baseline golden一致、compatibility facade identity、production import allowlistを回帰testにした。
- 現在できるようになったこと: #406はcatalogでBundleをresolveし、assembly時にtyped providerだけをexecution planへ渡せる。package移動やimport orderingがlogical identityまたはroot public objectを変えた場合はtestで検出できる。
- 実験的価値: 同一の固定manifestとsoftware revision identityに対するfreeze identityをbaseline goldenへ照合でき、package pathが研究条件のlogical identityへ混入していないことを継続検査できる。
- まだ言えないこと: runner、fixture playback、physics execution、性能差、participant結果、hardware safetyは実装・測定していない。
- 判断: 今回はcomposition / identity guardrailの強化であり、新しいexperiment conditionまたは観測結果を取得していないため`docs/experiment-notes/`は更新しない。
- 次の作業: #406はcompatibility facadeまたは`plugins.robots.fast_arm`を直接importせず、catalog、resolved composition、typed provider APIを使用する。

## 2026-07-17: #426 zero-core-change robot onboarding

- 日付: 2026-07-17
- 目的: multi-robot評価で新しいrobotを追加するたびにgeneric catalog、runtime、viewerへ具体実装を追加せず、repository first-party pluginとして検証・利用できる境界を成立させる。
- 実施内容: 固定`plugin.py` / `ROBOT_PLUGIN` registration、bounded deterministic discovery、declaration-owned asset / configuration validation、plugin-owned `viewer-robot-declaration/v1` deliveryを追加した。監査修正ではonboarding schema versionとrobot logical versionを分離し、logical v2 fixtureをProfile / Runtime Plugin / 全typed providerごと再assemblyした。`PluginSelection`をregistration、catalog projection、runtime compositionへ伝播し、providerのcanonical owner bindingとsymlink解決後のrobot固有resource ownershipを追加した。viewerは検証済みdeclaration resourceのURL + digestをframeで受け、connectionごとにfull declarationを一度だけ取得する。production catalogはdiscovery結果だけを投影し、fast_armのBundle / Profile / Runtime Plugin object identityを維持した。
- 検証: broken / missing / duplicate / identity mismatch / path escape / sibling direct reference / sibling symlink / root外symlink / URL-resource mismatch / VFS不足をfail-closedにし、fast_arm logical v1とfixture logical v2のmixed registry、public catalog resolver、resolved experiment composition、`RuntimeConfig`経由のheadless runtime reset / 1 step、全providerのstale owner / version mismatch、session digest固定、frame byte-size、viewer declarationのPython / TypeScript validation、fast_arm conformance、#405 manifest / readiness / freeze identity、architecture import boundary、full Python / viewer suiteを対象にする。最終command結果はIssue #426のDraft PRへ記録する。
- 現在できるようになったこと: production robot package、asset、configurationを自己完結して追加し、logical versionを失わずcatalog resolver、readiness composition、headless runtime、rendering-only viewerへ同じrobot declarationを渡せる。resourceとtyped providerのownership driftはstartup前に拒否できる。steady-state frame sizeはVFS / style件数に依存せず、reconnectではdeclarationを再取得できる。
- 実験的価値: multi-robot条件をpackage locationやdiscovery順から分離したlogical identityとして扱い、同じreadiness / freeze boundaryへ接続するための再現可能なonboarding基盤が成立した。
- まだ言えないこと: 第2robotはtest-only fixtureであり、新しいproduction robotの制御性能、物理妥当性、world / tool性能差、participant結果、hardware安全性を実装・測定・証明していない。
- 判断: Bundleはassembly boundary、viewerはrendering-onlyを維持する。external plugin、remote download、hot reload、runner lifecycleは導入しない。実験条件や観測結果を取得していないため`docs/experiment-notes/`は更新しない。
- 未解決事項: #406でdiscovered Bundleをreadiness後のrunner lifecycleへ接続し、実験record / outcome境界を実装する必要がある。
- 次の作業: #406は`discovery -> registration/resource validation -> Bundle resolution -> readiness/freeze -> runner`の順を維持し、必要なtyped providerだけをexecution edgeへ渡す。
- 関連Issue / PR / commit: Issue #426、blocked successor #406。baseline mainは`961d53047be26b4e0871fc3b22026024bec5de93`。Draft PRとfinal commit identityはGitHub metadataを正とする。

## 2026-07-17: #427 payload-first viewer input continuity correction

- 日付: 2026-07-17
- 目的: payload-first viewer bootstrap後もWebSocket接続とkeyboard / gamepad input publicationを継続できる状態を回復する。
- 実施内容: `connectionStatus`の更新をWebSocket lifecycleへ限定し、declaration fetch、model / VFS fetch、MuJoCo compile、scene構築はrenderer `status`だけを更新するようにした。keyboard / gamepad lifecycleを同じ`open` gateへ結線し、bootstrap中のstatus更新でsender / pollingをdisposeしない回帰testを追加した。
- 検証: deterministic browser fakeでpayload bootstrap相当の`open -> loading -> ready`状態を通過させ、keyboardのKeyW press / release sequenceとgamepadの継続publicationを確認する。実験条件・観測結果ではなく、viewer runtime capabilityの回帰検証である。
- 現在できるようになったこと: live viewer接続中にmodel declaration / WASM initializationが進んでも、表示frame受信とkeyboard / gamepad input publicationを同時に継続できる。
- 実験的価値: viewerからの連続inputを評価系へ渡せるsoftware pathがpayload-first onboarding後も失われないため、後続の入力連続性検証を同じ接続lifecycle上で再現できる。
- まだ言えないこと: この修正は実ブラウザ、backend liveness、hardware、command safety、実験条件の性能、participant結果を測定または証明しない。
- 判断: keyboard / gamepad mapping、cadence、deadzone、focus / visibility safety、transport schema、backend timeoutは変更しない。`docs/experiment-notes/`は実験条件・観測結果を取得していないため更新しない。
- 関連Issue / PR / commit: Issue #426、Draft PR #427、前回head `2d97e85be06136aec19752047388f2c471c7caef`。最終commit identityはGitHub metadataで確定する。

## 2026-07-17: #427 renderer bootstrap error input fail-safe correction

- 日付: 2026-07-17
- 目的: declaration / model bootstrapが失敗した状態で、connectionが`open`のままでもkeyboard / gamepad commandを継続送信しないfail-safe境界を成立させる。
- 実施内容: `liveInputEnabled = connectionStatus === "open" && status !== "error"`を共有state helperとして導入し、ProductViewerAppのinput effectをraw statusではなく安定したbooleanへ結線した。renderer `status=error`でsender、keyboard RAF、gamepad RAF / heartbeatを停止し、loading / ready / warning間ではsenderを再生成しないdeterministic regression testを追加した。
- 検証: malformed declaration、digest mismatch、model fetch、VFS fetch、MuJoCo compatibility failureを共通`status=error`境界へ写像し、connectionStatusを`open`に保持したままlive input availabilityがfalseになることを確認する。browser fakeではerror後のKeyW、gamepad RAF、heartbeat timerでsequenceが増えないことと、new open/loading sessionだけで再開することを確認する。
- 現在できるようになったこと: payload-first viewerが表示用model validationに失敗した場合、表示connectionの診断状態を保ったままcontrol publicationをfail-safe停止できる。正常なloading -> ready遷移では同一sender / pollingを継続できる。
- 実験的価値: renderer failureをcontrol pathへfail-openで流さず、正常表示可能性とlive input availabilityを分離したため、後続評価で「表示失敗中にcommandを送れる」状態を測定条件へ混入させないsoftware guardrailが成立した。
- まだ言えないこと: 実ブラウザでの意図的bad declaration / model URL、backend受信ログ、hardware、backend liveness、command safety、実験条件の性能、participant結果は測定していない。
- 判断: connectionStatus ownership、payload-first open維持、keyboard / gamepad mapping、cadence、deadzone、focus / visibility safety、transport schema、backend timeoutは変更しない。実験条件・観測結果を取得していないため`docs/experiment-notes/`は更新しない。
- 関連Issue / PR / commit: Issue #426、Draft PR #427、前回head `c0bf0b642141c54671488ffe851bfa65f81bb156`。最終commit identityはGitHub metadataで確定する。

## 2026-07-20: #459 Input Source Plugin P2 composition axis

### Review correction: runtime health and reader output contracts

- 目的: runtime readerからsource-owned current healthをtyped capabilityとして取得し、`read_frame()`結果をruntime boundaryでfail-closedに検証できるようにした。
- 実装能力: `InputSourceHealthProvider.current_health()`、factory直後の`initial_health` coherence、`ValidatedInputSourceReader`によるframe / healthの呼出しごとの型検証を追加した。初期確認はframe先読み、lifecycle start / close、hardware accessを行わない。
- lifecycle: offline / replayはmanaged lifecycleを要求せず、live / viewer_bridgeだけがmanaged adapterで`start()` / `close()`を透過委譲する。
- 位置付け: これはsoftware-only architecture capabilityの記録であり、実験結果、hardware validation、participant resultではない。payload projectionとconcrete source migrationはP3、viewer provider separationはP4に残る。

- 目的: Input SourceをRobot Bundle、Environment、Control Mapping、Task、Evaluationに続く第6のversioned composition軸として、generic contract / registry / startup readinessへ接続する。
- 実施内容: `PluginAxis.INPUT_SOURCE`、`InputSourcePlugin`、typed source mode、`read_frame() -> RawInputFrame` reader boundary、live / viewer_bridge限定のmanaged lifecycle、source-owned health、deterministic initial metadata、factory side-effect boundary、produced sample schemaを追加した。Control Mapping Pluginはaccepted sample schema identitiesを宣言し、composition rootでexact compatibilityをfail-closed検証する。manifest / registry / resolved composition / evidence producer bindingにも明示的なsource selectionを追加し、canonical evaluation manifestは`evaluation-manifest/v2`へ明示更新した。
- 現在できるようになったこと: source identityとsample schema identityを独立してversion管理し、unknown / duplicate / contract mismatch / parameter ownership mismatch / schema mismatch / registry type mismatchをstartup前に拒否できる。compositionはfactoryやlifecycle startを実行せず、source pluginとresolved schemaだけを保持する。
- 検証: generic in-memory conformance fixture、contract / registry / composition / architecture boundary、existing experiment composition / evaluation manifest、runtime / input_sources / architecture suiteをsoftware-onlyで検証した。
- 研究上の意味: source追加時のgeneric contract、deterministic registry、mapping compatibility gate、evidence bindingが成立し、Input Sourceを独立した比較・構成軸として扱えるようになった。これはarchitecture capabilityの検証であり、実験条件・観測結果・性能結果ではない。
- まだ言えないこと: programmed target、replay、noop、loadcell sourceのproduction plugin migrationは#460、viewer backend / keyboard / gamepad provider separationは#461、plugin-local test scopeとcompletion auditは#462へ残る。serial、browser、hardware、robot outputは実行していない。
- 判断: `docs/experiment-notes/`は新しいexperiment condition、model / fixture、execution command、observed resultを扱っていないため更新しない。
- 関連Issue / PR / commit: Issue #459、Draft PR title `[Input Source Plugin P2] versioned contract・registry・composition axisを追加する`。baseline main `54dd46226dd2276f0f8c436a4a347508a009353c`。

## 2026-07-20: #460 PR #465 review corrections

- 目的: PR #465のレビュー指摘5件を、既存backend input sourceのobservable behaviorを変更せずに修正する。
- 実装能力: noop readerの`RawInputFrame`戻り値、managed sourceのnormal / failure cleanup、live serialのread-before-start fail-closed、viewer typed ingress / rebase capability、programmed targetのpositive steps validationとterminal hold、selection / runtime readerのcanonical source-state projection parityを固定した。
- 検証上の意味: source pluginのhealth truthとruntime projection、managed lifecycle、viewer backend bridgeのcapability boundaryをarchitecture contractとして強化した。これはsoftware-only architecture capabilityの検証であり、実験条件・観測結果・hardware validationではない。
- 非対象: production catalogの7 IDs、sample schema identities、generic CLI aliases、`evaluation-manifest/v2`、viewer TypeScript、keyboard / gamepad mapping、viewer control message schema、loadcell protocolは変更しない。frontend provider分離は#461、test ownership再配置は#462へ残る。
- `docs/experiment-notes/`は新しいexperiment condition、fixture result、observed resultを扱わないため更新しない。real serial、Arduino、OSC、browser auto-open、robot outputは実行していない。

## 2026-07-20: #460 Input Source Plugin P3 backend source migration

- 目的: 既存backend input sourceをversioned production catalogとregistrationへbehavior-preservingに移行し、source追加時の変更範囲をplugin packageとcatalog registrationへ縮小する。
- 実装能力: programmed target、replay、noop、viewer backend、loadcell serial / fixture、analog fixtureの7件をplugin ID、contract version、sample schema、mode、CLI alias、execution adapter付きで登録した。selectionはaliasからtyped plugin、validated reader、initial health、runtime dependency、execution adapterを解決する。
- runtime境界: source固有validation、factory、initial metadata、health、managed lifecycleをregistration / plugin-local adapterへ閉じ、runtime selection / step-loop / compatibility scriptからsource ID conditionalを撤去した。healthは既存payloadの`source_active`、`command_age_ms`、`stale_reason`へprojectionする。
- 研究上の意味: source acquisition、sample schema、health、lifecycleをmapping・robot capabilityから独立したversioned composition軸として追加でき、source追加時にruntime coreを変更しない構成能力を確認できる。viewer frontend providerとkeyboard / gamepad mapping分離は#461へ残る。
- 検証: plugin / catalog、既存source、runtime selection / step-loop、viewer backend、loadcell fake / fixture、architecture、compile、Markdown、full Pythonおよびviewer toolchainをsoftware-onlyで検証する。これは実験条件・観測結果ではなくarchitecture capabilityの検証である。
- hardware / evidence: real serial、Arduino、OSC、browser auto-open、robot output、deployment、credential操作は行わない。`docs/experiment-notes/`は新しい実験条件・fixture結果・観測結果を扱わないため更新しない。
- 関連Issue / PR / commit: Issue #460、Draft PR title `[Input Source Plugin P3] 既存backend sourceをplugin registryへ移行する`。baseline main `de82da9720141455cf1f2990d50108a382f77500`。PR番号と最終commitはGitHub metadataで確定する。

## 2026-07-22: #461 viewer provider / source / mapping boundary

- 目的: browser keyboard / gamepad acquisitionとbackend viewer source、Control Mapping、runtime endpoint command compositionを独立して変更・検証できるarchitecture capabilityを成立させる。
- 実施内容: `keyboard/v1`と`gamepad/v1`のknown-ID provider registryをfrontendへ追加し、focus / visibility / disconnect、zero-state、timestamp / sequence、provider raw payload、lifecycle disposalをprovider ownerへ集約した。backend viewer sourceはprovider identity / schema、canonical `viewer_control_sample/v1`、latest sample、health、250 ms timeout、legacy message compatibilityを所有し、keyboard / gamepad mappingはversioned Control Mapping Pluginへ移した。runtimeはmapping resultの適用、desired endpoint progression、endpoint rebase、MuJoCo command compositionを維持した。
- 検証上の意味: 同じprovider raw sampleからkeyboard / gamepad mapping semanticsを再現し、既存axis sign、speed、deadzone、button 0/1 supplement、world / tool frame、key-up、stale、disconnect、final MotionCommand、endpoint rebase、payload metadataをsoftware-onlyで比較できる境界が成立した。これは新しいexperiment conditionや測定結果ではなく、browser device acquisitionとexperiment mappingを独立に組み合わせるためのarchitecture capabilityである。
- 研究上の意味: device acquisition identity、source health、mapping semantics、runtime physical commandを別のversioned ownerとして扱えるため、将来のprovider追加やmapping比較がsource実装の変更なしに構成可能になる。legacy messageはcanonical sampleへ一度だけ変換され、mappingの二重実装を作らない。
- まだ言えないこと: 実ブラウザ、real WebSocket、serial、Arduino、OSC、physical robot output、participant結果、性能差、実験条件の評価結果は取得していない。
- 判断: experiment condition、model / fixture、実験実行command、観測結果を新規取得していないため`docs/experiment-notes/`は更新しない。#462でplugin-local test ownership、dummy onboarding、legacy fallback retirement、retained compatibility symbolをcompletion auditする。
- 関連Issue / PR / commit: Issue #461、Parent #457、Depends #460、previous merged PR #465、required next issue #462。baseline mainと最終PR headはGitHub metadataを正とする。

## 2026-07-23: #461 audit corrections

- 目的: PR #466の監査で確認したprovider / source / mapping境界のP1・P2課題を、既存viewer入力のobservable behaviorを維持したまま解消する。
- 実施内容: keyboard captureのdisable / dispose reset、malformed ingressのsource-owned `invalid` transition、typed Control Mapping parameters、canonical `viewer_input_sample`単独mapping、frontend raw gamepad acquisition、sourceとmappingの独立`PluginSelection`、produced / accepted sample schema gateを追加した。runtimeのhold、endpoint progression、publish-before-rebase、MuJoCo command compositionは維持した。
- 研究上の意味: provider acquisition、source health、mapping semantics、runtime commandを独立した構成軸として選択・検証でき、legacy summaryをmappingのSoTにしないsoftware architecture capabilityを確認できる。これは新しいexperiment condition、model / fixture、観測結果、性能評価ではない。
- 検証: focused source / mapping / runtime / schema / architecture tests 53件、関連runtime / websocket / architecture回帰74件、CI対象Python 1020 passed / 10 skipped、compileall、strict Markdown（errors 0）、viewer unit、typecheck、production buildをsoftware-onlyで検証した。実ブラウザ、real WebSocket、serial、Arduino、OSC、physical robot outputは実行しない。
- 判断: `docs/experiment-notes/`は新しいexperiment condition、model / fixture、execution command、observed resultを記録していないため更新しない。#462のplugin-local test relocation、dummy onboarding、legacy retirementはfollow-up候補として残す。
## 2026-07-26: #461 P1/P2 audit correction follow-up

- 目的: #466監査で残ったgamepad activity / zero-state分離、mapping parameter readiness、SoT同期のP1/P2を限定的に解消する。
- 実施内容: `raw_axes`をmappingのcanonical inputとし、normalized `axes`はwire / overlay compatibility projectionとして残した。source activityをmapping deadzoneやcommand zeroから分離し、mapping parameter contractとoptionalなsemantic normalizationをselection / plan readinessでmanaged sourceのlifecycleとframe readより前に実行し、正規化済みparameterをstep loopへfrozen mappingとして渡す。
- 検証: focused Python viewer source / mapping / runtime testsは69 passed、full Pythonは1203 passed / 12 skipped、CI対象subsetは1036 passed / 10 skipped、strict Markdownはerrors 0、compileall、`git diff --check`、viewer `npm test`、typecheck、production buildはpassした。fresh adversarial auditでもraw / legacy compatibility、source healthとcommand zeroの分離、readiness timing、generic composition、#462 scope exclusionを確認した。
- 研究上の意味: default / legacy compatibilityを維持したsoftware-onlyのprovider / source / mapping構成境界を成立させる。experiment resultまたはhardware validationではない。
- 未検証事項: real browser、external WebSocket、serial、Arduino、OSC、physical robot output、deployment、credentialsは使用していない。#462のplugin-local test ownership、dummy onboarding、legacy fallback / retained-symbol auditは後続scopeとして残す。
- 関連Issue / PR / previous head: #461 / #466 / `2bc000751b4871b8a2657f49adfb8fe85944a740`。

## 2026-07-26: #461 final independent audit correction

- 目的: PR #466の独立監査で残ったdefault gamepad transfer-function parity、direct `ViewerInputSource` compatibility parameter precedence、SoT同期をbehavior-preservingに解消する。
- 実装能力: raw `raw_axes`をauthoritative mapping inputとして維持し、fixed frontend deadzone `0.1` projectionとlegacy backend thresholdの二段transfer functionをControl Mapping Plugin内で一元化した。default `0.1`ではraw `0.15` / `0.19`がzero、raw `0.20`がlegacyと同じ非zero、custom `0.0`でもraw `0.05`はprojected `axes=[0.0]`とlegacy `zero_state=true`によりholdとなり、raw `0.15`は`1/18`になる。direct source compatibility parameterは、明示されたruntime mapping parameter、source compatibility parameter、plugin defaultのprovenance順でplanへ渡す。
- 検証: positive / negative representative grid、effective threshold付近、custom deadzone、button / lifecycle safety、selection -> plan -> source -> mapping -> `MotionCommand`のcompatibility precedence、default parity runtime motion / holdをfocused Python 77 passedで確認した。full Pythonは1236 passed / 12 skipped、CI-compatible subsetは1069 passed / 10 skipped、compileall、strict Markdown（errors 0）、encoding（14 files / 0 issues）、`git diff --check`、viewer `npm test`、typecheck、production buildもpassした。GitHub Actions run `30206290531`は実装修正commit `9686e63d411b5af9a5ad3137f80e4fb8139e127e`でpython-validation / viewer-validationともsuccessとなった。以後のdocumentation-only synchronizationはruntime semanticsを変更しない。current PR headとlatest Actions runはGitHub PR metadata / Actionsを正とする。
- 研究上の位置付け: これはsoftware-onlyのprovider / source / mapping architecture capabilityと既存observable behavior parityの回帰修正であり、experiment condition、participant result、performance claim、hardware validationではない。real browser、external WebSocket、serial、Arduino、OSC、physical robot、deployment、credentialsは実行しない。
- handoff: remaining `input_sources` facade、plugin-local test relocation、dummy onboarding、legacy fallback / retained-symbol auditは#462 scopeとして残す。`docs/experiment-notes/`は新しいexperiment condition / observed resultを取得していないため更新しない。

## 2026-07-27: #462 Input Source Plugin P5 completion capability

- 日付: 2026-07-27
- 目的: Input Source Plugin Roundのtest ownership、reusable conformance、zero-core-change onboarding、compatibility境界をcompletion auditとして固定する。
- ownership correction: analog mapping、replay command compatibility、loadcell endpoint/command conversionのcanonical ownerを`plugins/mappings/`へ固定した。loadcellのparserとintrinsic normalizationはsource-ownedのまま、raw `loadcell_vector_sample/v1`をversioned `mapping_input_adapter` contract（normalized `loadcell_normalized_input_intent/v1`へ変換）でeffective mapping-input boundaryへ渡す構成を成立させた。
- readiness correction: source-produced schema、adapter input/output schema、Control Mapping accepted schemaを同じeffective schema ruleで`compose_experiment()`とruntime selectionが検証し、loadcell mapping parameterのsemantic normalizerをsource reader creationより前に共有する能力を固定した。
- conformance correction: production 7 source casesがmetadata、timestamp / sequence policy、terminal hold、health transitionをsource-local specとして宣言し、generic suiteがcallback/specを再利用して検証する。
- 実施内容: production source packageに対応するplugin-local test ownerを配置し、source固有parameterだけを注入するgeneric conformance helperを追加した。test-only dummy sourceをproduction catalog / CLI外のfixture catalogへ登録し、compatible / incompatible mapping、reader creation、composition readinessを検証した。runtime source-name fallbackを退役し、低位descriptor registryと未指定CLI runner defaultは既存compatibilityとして保持した。AST / import graph / registry introspectionによるarchitecture guardを追加した。adapter contractとmapping parameter normalizerをcomposition/runtime共通のstartup readinessへ接続した。
- 検証: plugin-local conformance、dummy onboarding、catalog / schema compatibility、runtime stale safety、architecture guardをsoftware-onlyで確認した。これは実験結果またはperformance claimではなく、source acquisition、mapping、runtime compositionを独立要因として追加検証できる構成能力の確認である。
- 現在できるようになったこと: 新しいsource固有実装をsource/plugin package、deterministic catalog registration、plugin-local tests、必要最小のdocsへ閉じ、runtime core、robot、task、evaluation、mapping algorithmを変更せずに契約適合性とcomposition readinessを確認できる。
- 実験的価値: plugin-local validation scopeとzero-core-change onboarding capabilityが成立し、Input Sourceを独立した研究条件として追加・比較する際の再現可能なsoftware boundaryを維持できる。
- まだ言えないこと: 新しいdeviceの性能、実験結果、participant result、hardware safety、real serial / browser / WebSocket behaviorは実装・測定・証明していない。
- 判断: この変更はInput Source Plugin Roundのcompletion capabilityを更新するためresearch log対象とした。unit test、compile、typecheck、build自体をexperiment evidenceとは扱わない。
- 未解決事項: PR #467のreview / mergeとIssue #462のcloseは未実施。Parent #457はP5 merge後の別明示操作までOpenのままとする。
- 次の作業: PR metadataでcurrent head、最新Actions、review stateを追跡し、merge後に#457のcompletion stateを別操作で監査する。mutableなhead / run identityはresearch logへ固定しない。
- 関連Issue / PR / commit: Issue #462、Parent #457、Draft PR #467。current headとActionsのmutable stateはGitHub metadataを正とする。

## 2026-07-29: #476 composition axis bounded discovery統一

- 日付: 2026-07-29
- 目的: Robotで成立していたself-contained packageとbounded deterministic discoveryを6 composition axisで監査し、新しいfirst-party pluginをgeneric coreの具体列挙変更なしに追加できるarchitecture capabilityへ整合させる。
- inventory: Robotは`ROBOT_PLUGIN` discovery済み、Input Sourceは7 packageにimplementationがある一方で中央registrationが具体列挙、Control Mappingは4 flat implementationと中央catalog列挙だった。Environment / Task / Evaluationはproduction concrete plugin / catalogがなく、generic typed contractとtest fixtureだけでsecond SoTは存在しなかった。
- 実施内容: Input Sourceへpackage-local `INPUT_SOURCE_PLUGIN`、Control Mappingへlogical IDと一致するpackage-local `CONTROL_MAPPING_PLUGIN`を追加し、direct-child、private除外、deterministic sort、fixed `plugin.py` importを行うbounded discoveryへcatalogを接続した。duplicate、missing / wrong export、broken import、package / logical identity mismatchはfail-closedとし、test namespaceのdummy packageでpackage追加だけのonboarding、production分離、package除去後のunknown identityを固定した。Robotは既存方式を維持し、Environment / Task / Evaluationにはproduction candidateがないため先行frameworkを追加しなかった。
- compatibility: Input Source 7 identity / schema / mode / health / execution adapter / default mapping、generic CLI 4 aliasと順序、Control Mapping 4 identity / algorithm / parameter / output、manifest / readiness / freeze、Robot Bundle、viewer lifecycle / safetyを変更しない。既存mapping import pathはpackage-owned implementationへのthin re-exportとして維持する。
- 研究上の意味: source、mapping、robotに続くfirst-party研究条件をpackage-local ownerとlogical identityに閉じ、generic catalog / runtime / viewer / unrelated pluginの具体列挙を変更せず同じstartup readinessへ接続できる。package locationとdiscovery順をexperiment identityから分離し、拡張条件の再現性を高めるsoftware architecture capabilityである。
- まだ言えないこと: 新しいproduction plugin、mapping性能、task/evaluator性能、participant result、physical妥当性、hardware safetyは実装・測定・証明していない。
- 判断: architecture capabilityが変わるためresearch logを更新する。新しいexperiment condition、model / fixture、実行command、観測結果を取得していないため`docs/experiment-notes/`は更新しない。
- external side effects: real serial、Arduino、OSC、physical robot、external browser automation、deployment、credentialsを使用しない。
- 関連Issue / baseline: Issue #476、baseline `5dac0ca694f44ac73417975b8b3034db68fdb121`。Draft PRと最終headはGitHub metadataを正とする。
