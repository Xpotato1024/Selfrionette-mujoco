---
status: historical
owner: research
last_verified: 2026-08-27
canonical_for: []
related:
  - research/README.md
  - docs/contracts/experiment-plugin-composition.md
  - docs/contracts/evaluation-manifest-readiness.md
  - docs/architecture/research-execution-roadmap.md
  - https://github.com/Xpotato1024/Selfrionette-mujoco/issues/495
  - https://github.com/Xpotato1024/Selfrionette-mujoco/issues/501
---

# 2026年8月 研究・実装ログ

## 2026-08-06: R7-G free-space用production 6軸composition

- 日付: 2026-08-06
- 目的: free-space endpoint評価のEnvironment、Task、Evaluation条件をversioned production pluginとして固定し、#406がtest-only fixtureなしで6軸compositionを受け取れるようにする。
- 実施内容: `free_space_environment/v1`、`endpoint_reach_task/v1`、primary `success_within_timeout/v1`、secondary `off_axis_drift/v1`、descriptive `completion_time/v1` / `final_endpoint_error/v1`を追加した。各axisのbounded discovery / catalogと、production catalogだけでworld/tool manifest pairを構築・readiness検証するboundaryを追加した。Taskはupper manifestからbindしたimmutable contextとMuJoCo world-frame measured sample / statusからdwell reset、timeout、technical-invalidを含むterminal classificationとcanonical evidenceをpureに生成する。
- 検証: plugin-local、discovery negative、production/test separation、README coverage、composition、manifest/freeze、world/tool pair、architecture/import boundaryのsoftware-only testsを実行する。最終commandと件数はDraft PRへ記録する。
- 現在できるようになったこと: Robot、Environment、Input Source、Control Mapping、Task、ordered Evaluation tupleとcommand semantics routeをproduction catalogだけから明示的にresolveし、Input Source生成やMuJoCo step前にreadiness / freezeと`EvaluationReadiness.task_execution_binding`を確定できる。single-target execution-candidate inputは50 sample × 0.02 s × 0.1 m/s = 0.1 mのnominal command budgetとterminal zero holdを持つ。
- 実験的価値: free-space endpoint reachのprimary / secondary / descriptive outcome identity、required canonical evidence、unit、frame、failure semanticsを再現条件として固定できる。
- まだ言えないこと: runner、実MuJoCo上のanalog playback、world/tool execution、4-target pilot、trial log、outcome artifact、measured reachability / success / drift / completion time / final endpoint error、metric妥当性、hardware safetyは実装・観測・証明していない。0.1 mのstatic budgetはmeasured reachabilityの証明ではない。
- 判断: target、tolerance、dwell、timeout、initial state、input / gain / deadzone / cadence、requested frameはupper evaluation manifestを正本とし、Task / Evaluation pluginへ重複parameterを持たせない。runnerはclassificationを作成せず、Task-owned transitionのterminal / trajectory evidenceを利用する。missing / unavailable / invalid measurementはsuccessまたは数値0へ変換しない。
- 未解決事項: #406のrunnerがproduction-resolved compositionを受け取り、#407のcanonical trial logを生成し、#408がlogからoutcome artifactを再構成する必要がある。
- 次の作業: #406でsoftware-only runner、analog fixture playback、world/tool executionを実装する。
- 関連Issue / PR / commit: Issue #495。Draft PRとcommit identityは作業完了時のGitHub metadataを正とする。

## 2026-08-20: R7-G software-only world/tool experiment runner

- 日付: 2026-08-20
- 目的: #495までに成立したproduction 6軸compositionを、analog fixtureからMuJoCo measured endpointとTask-owned transitionまでの有限・決定的なsoftware-only executionへ接続する。
- 実施内容: canonical world/tool manifest pairをproduction readiness / freezeへ通し、selected Environment、Input Source、Control Mapping、command route、Robot Bundleのtyped provider、MuJoCo、TaskExecutionBindingを結線するrunnerとthin module entry pointを追加した。manifest revisionとactual execution identityは独立入力として照合し、reset後のactual qpos / measured tool orientationをfrozen manifestへ検証する。providerはassembly時に一度だけ取得し、manifest cadenceのsimulation timeとtimeout由来のstep上限で実行する。
- 実測した範囲: canonical single-target fixtureを実MuJoCoで反復実行した。worldはTask-owned `success`へ57 step / 1.14 sで到達し、toolはTask-owned `failure`へ250 step / 5.00 sで到達した。両conditionでreset直後のmeasured initial endpoint handoffは`technical_invalid`にならず、同じrevision / manifest / conditionの再実行でclassification、step count、elapsed time、measured trajectory、terminal evidenceが一致した。
- failure semantics: stale source、source read failure、freeze後manifest変更、malformed fixture、invalid control-frame projection、qpos / command rejection、bounded stopをfail-closedで検証する。hold、stale、rejected、unavailable、invalidをnominalまたはsuccessへ変換しない。
- 現在できるようになったこと: hardwareやwall-clock pacingなしで、production 6軸selectionだけからworld/tool両conditionを実行し、TaskTransitionのclassification / canonical evidenceを改変せずin-memory resultとして取得できる。Evaluation Pluginはresolveするがmetricを実行しない。
- まだ言えないこと: `experiment-motion-log/v1` lifecycle、metric aggregation、CSV / JSON artifact、full deterministic E2E / completion audit、4-target pilot、participant study、metric妥当性、hardware safetyは未実装・未証明である。今回のcanonical fixture実行を研究pilot結果として保存・解釈しない。
- Experiment evidence impact: MuJoCo load / stepとrunner behaviorの確認は通常のautomated integration / software smokeであり、研究experiment data collectionではないため`docs/experiment-notes/`は更新しない。
- 次の作業: #407でtrial lifecycle recordingを接続し、#408でmeasured outcome / artifact、#409でdeterministic E2E / completion auditを行う。
- 関連Issue / PR / commit: Issue #406。Draft PRとcommit identityは作業完了時のGitHub metadataを正とする。

## 2026-08-20: R7-G experiment-motion-log/v1 trial lifecycle

- 日付: 2026-08-20
- 目的: #406のproduction world/tool software-only execution factsを、既存`experiment-motion-log/v1`のconfiguration、trial start、motion sample、trial outcomeへtruth levelを混同せず接続する。
- 実施内容: runnerの既存loopへimmutable execution-step traceを追加し、別のruntime recorderがrequested input、resolved frame、policy prediction、MuJoCo measured qpos / tip、独立status、Task-owned terminal classificationをv1 recordへprojectionする。configuration IDはconditionごとの`FreezeRecord.identity`へ直接bindし、protocol contextはcallerから明示的に受ける。complete streamだけをstrict round-trip validation後にUTF-8 without BOMのatomic JSONLとして保存する。
- 検証: canonical world successとtool timeout failureを実MuJoCoで記録し、record lifecycle、trajectory continuity、dwell proof、deterministic JSONL bytes、retry chain、partial no-commit、既存artifact保護、strict read-backをsoftware-only testで確認した。最終commandと件数はDraft PRを正とする。
- 現在できるようになったこと: 同じfrozen manifest、software revision、protocol context、fixtureから、world / tool trialのrequested / resolved / predicted / measured / status evidenceとTask outcomeを再構築可能なv1 JSONLとして決定的に生成できる。
- 実験的価値: execution configurationとtrial identity、sample trajectory、terminal classificationを同一record streamへtraceでき、#408でmeasured outcomeを再構成する入力境界が成立した。
- まだ言えないこと: Evaluation Pluginによるmetric artifact、off-axis drift / completion-time / final-error summary、condition summary、CSV report、cross-trial aggregation、inferential statistics、full #409 E2E、4-target pilot、participant study、metric妥当性、hardware safetyは未実装・未証明である。
- 判断: `experiment-motion-log/v1`のfield、unit、frame、nullability、enum、retry、success validationは変更せず、schema moduleをpureに維持する。loggingのためにMapping、motion policy、MuJoCo step、Task classificationを再実行しない。missing measurementはnullとreasonで保持し、requested / predicted valueまたはzeroで補わない。
- Experiment evidence impact: automated integration / logging smokeとしてMuJoCo load / stepを行うが、研究pilotのcondition / resultとして保存・解釈しないため`docs/experiment-notes/`は更新しない。
- 未解決事項: #408のmeasured outcome / evaluation artifactと#409のdeterministic E2E / completion auditが残る。
- 次の作業: #408でv1 logからmeasured outcomeを導出し、#409でfull deterministic E2Eとcompletion auditを行う。
- 関連Issue / PR / commit: Issue #407。Draft PRとcommit identityは作業完了時のGitHub metadataを正とする。

## 2026-08-23: R7-G measured evaluation artifact

- 日付: 2026-08-23
- 目的: #407のvalidated `experiment-motion-log/v1`から、requested / predictedではなくmeasured factsだけを使ったtrial metricと決定的なmachine-readable artifactを再構成する。
- 実施内容: `runtime/evaluation/artifact.py`をcanonical ownerとして追加した。入力streamをstrict validationし、readinessのconfiguration / freeze / manifest identityとsource logを照合し、configuration initial measured tipとordered measured endpoint samplesからTask-owned trajectory evidence equivalentを再構成する。terminal/sample elapsedはtrial start基準へrebaseし、complete sample後のtechnical-invalidもtrajectory invalidとして扱う。readinessがfreezeする15-field comparison projectionをrecorderと共有し、production compositionのordered `success_within_timeout/v1`、`off_axis_drift/v1`、`completion_time/v1`、`final_endpoint_error/v1`へgeneric delegationする。artifactはschema/version、source SHA-256、software revision、trial、condition、ordered evaluator identity、production declarationと結合したtyped metric status/value/unit/frame/provenance/reason、descriptive condition summaryをsorted compact JSONとして保存し、round-trip / read-back / persistent sidecar kernel lock付きatomic overwrite protectionを通す。sidecarはPID / JSON owner metadataを持たないinert operational stateとして残し、empty contentを変更せず、POSIXのowner-only mode要求とWindowsのcurrent OS security semanticsを分け、kernel handle closeで正常終了・失敗・crash時にlockを解放する。
- 検証: focused artifact testsは11 passed。canonical worldは57 samples / 1.14 s / success、toolは250 samples / 5.00 s / failureを維持した。parallel-shifted elapsed、technical-invalid trajectory、15-field tamper、evaluator declaration tamper、malformed / empty sidecar contentの無視、process-local thread exclusion、concurrent process exclusion、crash/reopen、precommit failureとtwo-writer raceをfail-closedで確認し、同一source logのidentity binding、strict decode/round-trip、再encode byte equality、canonical bytes SHA-256を再現性testで確認した。
- 現在できるようになったこと: 同じvalidated source logとreadinessから、同一bytesの`evaluation-artifact/v1`を再生成し、Task evidenceと#406のTaskTransition evidenceをsemantic-equivalentとして検証できる。invalid / unavailable evidenceをsuccess、fake trajectory、またはzeroへ変換しない。
- 実験的価値: software-only executionの測定結果を、source identity・configuration・provenance・metric policyと一体化した再現可能なartifactとして監査できる。これは評価入力と導出結果の境界を固定するもので、metric formulaの変更や条件のretuneではない。
- まだ言えないこと: #409 full deterministic E2E / completion audit、participant study、inferential statistics、superiority、pilot、hardware safety、metric妥当性は実装・観測・証明していない。canonical smokeはsoftware-only automated validationでありformal pilot evidenceではない。
- 判断: target、tolerance、dwell、timeout、gain、fixture、formula、requested control frameは変更していない。failed completion timeはunavailableとし、required evidence unavailable / technical-invalidはfail-closedで扱う。
- 未解決事項: #409でlog-to-artifactを含むfull E2E、completion audit、全canonical suiteの統合確認を行う必要がある。
- 次の作業: #409でbounded full E2E / completion auditを行う。artifact schema v2、participant / inferential evaluation、hardware validationは別scopeとする。
- Experiment evidence impact: 実行結果はautomated software-only smokeでありpilotのcondition / resultとして保存・解釈しないため`docs/experiment-notes/`は更新しない。
- 関連Issue / PR / commit: Issue #408。Draft PRとcommit identityは作業完了時のGitHub metadataを正とする。

## 2026-08-23: R7-G deterministic software-only E2E audit candidate and review correction

- 日付: 2026-08-23
- 目的: #405〜#408のcanonical manifest、production six-axis readiness、world/tool MuJoCo execution、`experiment-motion-log/v1`、Task evidence reconstruction、production Evaluation Plugin、`evaluation-artifact/v1`を一つの有限deterministic E2Eとして再現し、R7-Gの到達範囲と次段階へのhandoffを固定する。
- 実施内容: `selfrionette-r7-g-e2e --output-dir <absolute-directory> --manifest-software-revision <declared> --execution-software-revision <observed>`を追加し、manifest revisionとcaller-observed actual execution revisionを独立入力としてreadinessでexact matchさせる。同一revision / manifest / protocol contextで2回実行し、既存runner、recorder、evaluator、artifactのowner APIだけを順番に呼び出し、canonical log / artifactのstrict decode、round-trip、read-back、output namingを検証した。readiness mismatch、malformed log、held / rejected / stale、measurement unavailable、technical-invalid、artifact identity mismatchをnegative controlとしてfail-closedに確認した。sample-onlyの4 controlは成功outcomeを変更せず、v1 validatorの不整合拒否を通す。
- 検証: explicit fixture `test-revision:issue-409-fixture`をWindows x64 / CPython 3.12.13 / MuJoCo 3.12.0へ渡した。worldは57 samples / 1.14 s / success、toolは250 samples / 5.00 s / failure。motion logは313 records、597159 bytes、SHA-256 `8a6da45eef06a090dd52012f4c579a09b31629ed305cf553d978cfe61155b14e`。world artifactは3044 bytes / SHA-256 `804af2bf79ddbd69f211f430592afd603e878ba95ba0cc29b2f022001ddd546a`、tool artifactは3111 bytes / SHA-256 `5ec0c003e863937f6d5db2fab02c9395b10e7f56b5c7fe5d91eb5fe2b71c72d3`。同一環境の反復runでexecution classification、step count、simulation time、evidence、metric、artifact bytesが一致した。OS / 依存版を跨ぐartifact bytes / SHA-256一致は仕様値としない。
- 現在できるようになったこと: callerが明示したmanifest / actual revisionをreadinessで照合し、固定したfree-space world/tool software-only条件をproduction compositionからlog、measured Task evidence、ordered evaluator、deterministic artifactまで一つの有限commandで監査できる。失敗、unavailable、technical-invalidをsuccessまたはzeroへ変換しない。
- 実験的価値: 実験条件と導出結果のbyte-stable traceability、negative boundary、software-only再現性を一つのcompletion evidenceとして確認できる。これはformal pilot dataではなく、metric妥当性や写像の普遍的優越を示すものではない。
- まだ言えないこと: mapping普遍的優越、participant performance、NASA-TLX、4-target participant pilot、inferential statistics、live hardware、authoritative physical safety、contact task、physical robot output、long-duration robustnessは未証明である。R7-H #410、R7-I #418、R7-J #419、R7-K #420へhandoffする。
- 判断: #404 / #293のclose-ready metadataは独立reviewとparent側のlocalized updateまで変更しない。`docs/experiment-notes/`はparticipant / formal pilotの条件・観測を取得していないため更新しない。
- 未解決事項: #499/#500はOpen/Draftであり、独立reviewのP0=0、P1=2、P2=1を修正して再reviewする必要がある。#404/#293 metadataはparent-first merge後まで更新しない。artifact schema v2、participant / inferential evaluation、hardware validationは別scopeとする。
- 次の作業: revision identity / sample-only negative controlの修正を再validationし、implementation stackの再review後にmerge-order readinessを判断する。R7-H contact task、R7-I participant pilot、R7-J physical safety、R7-K persistent runtime / robot output gateは各Issueのscopeとoperator gateに従う。
- Experiment evidence impact: automated software-only E2Eでありformal pilotのcondition / resultを取得・保存していないため`docs/experiment-notes/`は更新しない。
- 関連Issue / PR / commit: Issue #409。Draft PRとcommit identityは作業完了時のGitHub metadataを正とする。

## 2026-08-27: 実機接触bring-upを最優先とする研究実行方針

- 日付: 2026-08-27
- 目的: R7-G完了後の研究実行順を、software-only completenessの積み上げではなく、Selfrionette / gamepadでfast_arm実機を安全に操作し、接触タスクを成立させるmilestoneへ最短で到達する順序へ固定する。
- 実施内容: current mainとR7-H / R7-I / R7-J / R7-K、production Input Source / Mapping ownershipを再確認した。`selfrionette/v1`と`viewer/v1`は既にproduction input pathを持つため、次の主課題をdevice追加ではなくcontact-core、physical safety、minimal physical output gateと定義した。currentな詳細は`docs/architecture/research-execution-roadmap.md`、判断理由はADR 0004へ分離した。
- 検証: R7-G / #404 completed、live `selfrionette/v1`、viewer gamepad bridge、R7-H #411〜#417、R7-I #418、R7-J #419、R7-K #420のactual current scopeを照合した。実機、serial、OSC、robot outputは実行していない。
- 現在できるようになったこと: 次の実装を`R7-H contact-core (#411 -> #412 -> #413 -> #415)`とR7-J physical safety coreの並行trackとして開始し、R7-J gate後にR7-K minimal physical output、gamepad free-space、Selfrionette free-space、bounded physical contactへ進む判断基準を一意に参照できる。
- 実験的価値: 実機接触という研究上の主要riskを早期に検証し、入力経路、physical output、contact taskのfailure sourceを段階的に切り分けられる。Gamepadはbring-up / control baselineであり、主比較を入力装置間比較へ変更しない。
- まだ言えないこと: physical feasibility、実機安全性、gamepad / Selfrionette実機操作、contact stability、mapping優越、participant performance、NASA-TLX、反力提示効果は未検証である。
- 判断: #414 virtual reaction-force、#416/#417 full software-only completion、R7-K service / containerは価値を維持するが、最初のmanual physical contact smokeのhard blockerにはしない。formal pilot開始前にはcontact logging / provenance、protocol、retry / exclusion ruleを別途成立させる。実機作動は専用Issueと明示許可を必須とする。
- 未解決事項: R7-J safety-coreのchild Issue設計、R7-K minimal physical output gateのchild Issue設計、R7-H #415 dependencyのreconcile、actual hardware device / command / stop / rollback条件の固定が必要である。
- 次の作業: roadmap metadataをreconcileした後、R7-H contact-coreとR7-J safety-coreの最初のIssueを、実装可能な1 Issue / 1 PR単位で開始する。
- 関連Issue / PR / commit: Issue #501。repository docsのmerge前はDraft PRのGitHub metadataを正とする。

## 2026-08-28: R7-J-P1 physical limit evidence contract

- 日付: 2026-08-28
- 目的: physical output gateが、実機資料・実測・software projectionを混同せずlimit evidenceを参照できる入力境界を固定する。
- 実施内容: `physical-safety-envelope/v1`として、position / velocity / accelerationのlimit、joint / motor / actuator space、unit / frame、source・revision・evidence reference、conversion provenance、authoritative / provisional / unknown / unavailable / conflict / invalidをtypedに保持し、決定的JSON serializationとstrict validationを追加した。`joint_limit_toml`、Robot Profile、MuJoCo `jnt_range`等のsoftware sourceはprovisionalとして扱い、physical authorityへ自動昇格しない。
- 検証: hardwareなしのfixtureで、明示physical provenance付きvalid envelope、欠落sourceのunknown、software sourceのauthority拒否、conflict / invalid、BOM / unknown field、round-trip / deterministic bytesを確認する。
- 現在できるようになったこと: 後続P2〜P6が、値とprovenanceを一体にしたversioned envelopeを読み、未証明領域をbounded authorityとして誤使用しない入力契約を参照できる。
- 実験的価値: software feasibility boundaryとphysical evidenceの境界を再現可能に表現できる。これは実機のlimit値を取得したことを意味しない。
- まだ言えないこと: fast_armのauthoritative joint / motor / actuator range、mechanical stop、cable constraint、self-interference、environment clearance、dynamic feasibility、physical safety、実機動作はいずれも未検証である。
- 判断: 現行TOML、Robot Profile、MuJoCo modelを変更せず、missing / conflict / unavailableは後続gateでfail-closedにする。実測は#509の明示許可まで行わない。
- 未解決事項: P2でspace conversionとProfile / TOML / MuJoCo parityを照合し、resolved boundsがauthoritativeかprovisionalかを保持する必要がある。
- 次の作業: R7-J-P2 (#504)でjoint / motor / actuator mappingとread-only resolved bounds providerを追加する。
- 関連Issue / PR / commit: Issue #503。Draft PRとcommit identityはGitHub metadataを正とする。

## 2026-08-28: R7-J-P2 fast_arm limit resolution and parity

- 日付: 2026-08-28
- 目的: physical limit sourceをfast_armのjoint / motor / actuator identityへ追跡し、後続safety gateがsoftware projectionとphysical authorityを区別して参照できるようにする。
- 実施内容: explicit gear / sign / offset relationによるjoint-space projection、sourceごとのparity record、resolved authoritative / provisional / mismatch / unknown / unavailable / invalid result、read-only `FastArmResolvedBoundsProvider`を追加した。既存TOMLはprovisional、MuJoCo `jnt_range`とProfile projectionもsoftware-onlyとして扱い、mapping欠落・unlimited model・source conflictは値を補完せずfail-closedにした。
- 検証: hardwareなしのfixtureで、正負signとoffset、missing conversion、matching provisional、explicit authoritative、range mismatch、unknown source、既存TOML projection、default fast_arm modelのunavailable range、invalid conversionを確認する。
- 現在できるようになったこと: 後続P3〜P6が、joint-spaceのnormalized boundとsource/parity/reasonを一つのtyped resultから取得し、Profile / TOML / MuJoCoの一致だけをphysical authorityと誤認しない。
- 実験的価値: software modelとphysical evidenceの差分、space conversionのidentity、未解決sourceを同じ監査結果で追跡できる。これはfast_armの実機rangeを確定したことを意味しない。
- まだ言えないこと: authoritative motor / actuator mapping、mechanical stop、cable constraint、self-interference、environment clearance、dynamic feasibility、physical safety、実機動作はいずれも未検証である。
- 判断: current Robot Profile、joint-limit TOML、MJCF、qpos guardを変更せず、unresolved resultは後続gateでallowへfallbackしない。physical measurementは#509の明示許可まで行わない。
- 未解決事項: P3でMuJoCo geometry inventory、self-interference / environment clearance、exclusion provenanceをcollision policyへ接続する必要がある。
- 次の作業: R7-J-P3 (#505)でconfiguration / bounded trajectory collision safety policyを追加する。
- 関連Issue / PR / commit: Issue #504。Draft PRとcommit identityはGitHub metadataを正とする。

## 2026-08-28: R7-J-P2 identity / unit review correction

- 日付: 2026-08-28
- 目的: P2のspace projectionとparityがsource / target identityおよびunitの不一致をbounded resultへ混入させないことを明確化する。
- 実施内容: `source_name`と入力limit name、conversion targetとcanonical joint nameを必須照合し、重複・曖昧なrelationを拒否する。unitは暗黙変換せず完全一致を要求し、parity identityへunitを含める。
- 現在できるようになったこと: 同じ数値でもunitまたはidentityが異なるsourceを`mismatch` / `unknown`として閉じ、別sourceの値やTOMLをphysical authorityへ補完できない。
- 実験的価値: software projectionの比較条件とfailure boundaryを決定的に追跡できる。
- まだ言えないこと: motor / actuatorの実機mapping、unit conversionの実証、physical range、physical safety、実機動作はいずれも未検証である。
- 判断: 明示的なunit conversion evidenceがない不一致は拒否し、#509のphysical measurement境界を越えない。
- 未解決事項: P3以降のcollision / dynamic gateと、実機資料に基づくmapping validationが必要である。
- 次の作業: #505でself-interference / environment clearanceのtyped collision policyを追加する。
- Experiment evidence impact: software-only unit / identity negative-control testであり、実験条件・観測結果は取得していないため`docs/experiment-notes/`は更新しない。
- 関連Issue / PR / commit: Issue #504。レビュー修正のDraft PR metadataを正とする。

## 2026-08-30: R7-J-P2 unit resolution correction

- 日付: 2026-08-30
- 目的: non-radのjoint limitをnormalized `rad` boundとして誤って解決しない境界を固定する。
- 実施内容: mixed unitは`mismatch`、同一non-rad unitは`unknown`としてboundを持たせず、全sourceが`rad`の場合だけrange比較・resolutionを行うようP2 resolverと`ResolvedJointBound`の不変条件を補正した。暗黙のunit変換は行わない。
- 現在できるようになったこと: singleまたは複数sourceのnon-rad値を未解決のまま追跡し、resolved resultのparity unit・bound・reasonをfail-closedに検証できる。
- 実験的価値: software projectionのunit前提を明示し、degree値をradian値として研究上の安全境界へ混入させない。
- まだ言えないこと: physical authorityは0件のままであり、fast_armの実機range、physical safety、実機動作はいずれも未検証である。
- 判断: downstream `physical_safety_core`のregression validationはcorrected tipで実施するため、本PRへbackportしない。本entryには詳細なtest logを記録しない。
- 未解決事項: corrected tipでのdownstream regression validationと、実機資料に基づくmapping validationが必要である。
- 次の作業: #505以降のsafety gateがこの未解決境界を保持することをcorrected tipで確認する。
- 関連Issue / PR / commit: Issue #504 / PR #522。レビュー修正のDraft PR metadataを正とする。

## 2026-08-28: R7-J-P3 collision safety policy

- 日付: 2026-08-28
- 目的: self-interference、environment collision、structural proximity、task-object contactを混同せず、bounded software checkをphysical output前のfail-closed boundaryへ接続する。
- 実施内容: explicit MuJoCo geometry role inventory、single-pair exclusion provenance、meter clearance / penetration / contact semantics、`clear` / `near_collision` / `collision` / `contact` / `unknown` / `unavailable` / `invalid` result、configuration check、bounded trajectory first-failure checkを追加した。unknown role、missing observation、global wildcard exclusion、根拠なしenvironment exclusionは許可しない。
- 検証: hardwareなしのfixtureで、self / environment classification、near-collision threshold、missing observation、provenance付きstructural exclusion、wildcard / non-structural exclusion拒否、unknown role、task-object contact、bounded trajectory stopを確認する。
- 現在できるようになったこと: 後続P4〜P6が、明示的geom pairとdistance evidenceに基づくcollision safety resultを参照し、collision未観測やrole不明をclearとして扱わない。
- 実験的価値: software model上のself-interference / environment clearance rejection classと、そのprovenanceを有限かつ再現可能に監査できる。これはmesh非接触、workspace全体、または実機安全の証明ではない。
- まだ言えないこと: authoritative physical limit、mechanical / cable constraint、実機collision clearance、dynamic feasibility、physical safety、実機動作はいずれも未検証である。
- 判断: collision exclusionsを既存MJCFへ追加せず、viewer側判定も作らない。#413成立後のcontact identityはexplicit task-object boundaryへ接続し、#509の明示許可までhardware validationを行わない。
- 未解決事項: P4でjoint limit / collision結果と独立したvelocity、acceleration、cadence、Jacobian singularity gateを追加する必要がある。
- 次の作業: R7-J-P4 (#506)でconfiguration / trajectory dynamic feasibilityを実装する。
- 関連Issue / PR / commit: Issue #505。Draft PRとcommit identityはGitHub metadataを正とする。

## 2026-08-28: R7-J-P3 body-role overlap review correction

- 日付: 2026-08-28
- 目的: geometry inventoryのbody identityへ異なるroleを重複割り当てした場合に、collision resultをclearと誤認しない。
- 実施内容: 同じbodyのrole集合重複をinventory validationで`invalid`とし、MuJoCo builderでもrobot / tool / environment / task objectの入力集合重複を拒否する。
- 現在できるようになったこと: role identityが曖昧なinventoryを後続P4〜P6へ渡さず、collision判定をfail-closedに停止できる。
- 実験的価値: geometry roleの前提とcollision classificationのfailure boundaryを決定的に監査できる。
- まだ言えないこと: model mesh全体、実機clearance、mechanical constraint、physical safety、実機動作はいずれも未検証である。
- 判断: role重複をprecedenceで解消せず`invalid`とし、既存MJCFやexclusionを変更しない。
- 未解決事項: P4でdynamic feasibilityとcollision resultの統合前診断を追加する必要がある。
- 次の作業: R7-J-P4 (#506)でvelocity / acceleration / cadence / Jacobian diagnosticsを追加する。
- Experiment evidence impact: software-only inventory negative-control testであり、実験条件・観測結果は取得していないため`docs/experiment-notes/`は更新しない。
- 関連Issue / PR / commit: Issue #505。レビュー修正のDraft PR metadataを正とする。

## 2026-09-04: R7-J-P2 effective evidence status correction

- 日付: 2026-09-04
- 目的: P2のresolved boundが`PhysicalLimit.status`だけでsource authorityやbounded parityを成立させない境界を固定する。
- 実施内容: limitとtyped sourceのstatusを`INVALID`、`CONFLICT`、`UNAVAILABLE`、`UNKNOWN`の優先順位で合成するcanonical helperを追加し、source未解決のprovisional value、typed authorityのないresolved authoritative bound、空またはidentity不一致のparity、期待joint集合の欠落・重複、source / relation identityが重複したconversion relation、`JOINT` conversion relationをfail-closedにした。同一jointへ異なるsourceを投影するparityは許可する。既存joint valueはidentity provenanceを保持し、`MOTOR` / `ACTUATOR`だけを明示conversionする。
- 研究上の意味: software projectionとphysical authorityの証拠状態を一つのtyped判定へ集約し、後続P3以降がsource statusを再実装せず同じ未解決境界を参照できる。これはphysical rangeやauthorityを新規取得したことを意味しない。
- まだ言えないこと: fast_armのauthoritative joint / motor / actuator range、mechanical stop、self-interference、dynamic feasibility、physical safety、実機動作はいずれも未検証である。
- Experiment evidence impact: unit / contract regressionのみで実験条件・観測結果を取得していないため`docs/experiment-notes/`は更新しない。
- 関連Issue / PR / commit: Issue #504 / PR #522。corrected detached implementation tipのcommit identityは作業完了時のGit stateを正とする。

## 2026-09-04: R7-J-P2 source authority correction

- owner: `runtime/safety/physical_limits.py`
- correction: source kind、authority assertion、authoritative source/revision/evidence identityをcanonicalに検証し、case variant・placeholder・synthetic authorityをfail-closedにした。具体的なmanufacturer/physical evidenceは保持する。
- research boundary: software contract correctionであり、physical numeric authorityは追加していない。`docs/experiment-notes/`は変更しない。

## 2026-09-04: R7-J-P2 resolution integrity correction

- owner: `runtime/safety/physical_limits.py`, `runtime/safety/limit_resolution.py`
- correction: physical authority kindを狭いallowlistへ固定し、source / limit / conversion / parity / bound / resultをcanonical deep revalidateするsnapshot binding、typed source identity、期待joint inventory、具体的conversion coverage、単一tolerance上限を追加した。bypassやnested差し替えはauthorityへ昇格しない。
- research boundary: software-only contract hardeningであり、physical numeric authorityは0件のまま。`docs/experiment-notes/`は変更しない。

## 2026-09-04: R7-J-P2 resolution identity and envelope correction

- owner: `runtime/safety/physical_limits.py`, `runtime/safety/limit_resolution.py`
- correction: projected conversion provenance now carries the typed input limit name and result binding requires relation/source-name equality; one canonical concrete-identity validator covers conversion identities, while external identity-plus-semantic seals detect nested same-value replacement, constructor bypass, private snapshot rewriting, and empty or mutated safety envelopes at public serialization/accessor boundaries.
- research boundary: software-only contract correction; physical numeric authority remains 0 and `docs/experiment-notes/` is unchanged.

## 2026-09-04: R7-J-P2 public resolution identity correction

- owner: `runtime/safety/limit_resolution.py`
- correction: `robot_id`、`expected_joint_names`、joint identityを単一のP2 canonical validatorへ接続し、空・whitespace・placeholder identityをconstructor、resolver、source collector、provider factory / accessorでfail-closedにした。通常のconcreteなrobot IDとjoint名は維持した。resultの既存external sealはidentity fieldsを含むため、constructor bypassや事後のplaceholder差し替えもauthorityへ昇格しない。
- research boundary: software-only contract hardeningであり、physical numeric authorityは0件のまま。実験条件・観測結果は取得していないため`docs/experiment-notes/`は更新しない。

## 2026-09-04: R7-J-P2 conversion JSON type correction

- owner: `runtime/safety/physical_limits.py`
- correction: `PhysicalSafetyEnvelope`のJSON decoderがconversionの`gear_ratio`、`sign`、`offset`をidentity比較へ渡す前にconstructor共有のstrict numeric validatorを通すよう補正し、Pythonの`bool`と`1.0` / `0.0`の等値によるidentity誤認を拒否する。
- research boundary: malformed software contract入力のfail-closed境界のみを補正し、physical numeric authority、実験条件、観測結果は変更しない。`docs/experiment-notes/`は更新しない。

## 2026-09-05: R7-J-P2 projected limit construction-origin correction

- 日付: 2026-09-05
- 目的: canonicalなmotor / actuator projection provenanceを、directな`JOINT` valueへ誤接続できない構築経路として固定する。
- 実施内容: 公開`PhysicalLimit` constructorでは`LimitConversionProvenance.projected()`の直接添付を拒否し、projection call-siteとJSON decoderだけをprivate canonical factoryへ接続した。limitのconstruction-origin sealをdeep validatorでも検証し、constructor bypassや事後のnested差し替えをdownstream authorityから除外する。既存のdirect `JOINT` identityと`MOTOR` / `ACTUATOR` projectionは維持した。
- 現在できるようになったこと: joint-spaceのidentity valueはconversionを再適用せず直接比較し、明示projectionだけがtyped non-identity provenanceを保持する境界を、serialization・resolution・authority判定で一貫して確認できる。
- 実験的価値: software projectionとjoint identityの取り違えを再現可能なnegative controlとして閉じ、後続safety gateが構築経路の曖昧さを数値一致だけで見逃さない。
- まだ言えないこと: authoritative physical numeric evidenceは0件であり、motor / actuatorの実機mapping、physical range、physical safety、実機動作はいずれも未検証である。
- 判断: これはsoftware-only contract correctionであり、`docs/experiment-notes/`は更新しない。direct constructor、forged provenance、decoder、status / unit / inventory regressionを同じP2 boundaryで検証する。
- 関連Issue / PR / commit: Issue #504 / PR #522。Draft PRとcommit identityはGitHub metadataを正とする。

## 2026-09-05: R7-J-P2 malformed MuJoCo range fail-closed correction

- owner: `runtime/safety/limit_resolution.py`
- correction: `fast_arm_mujoco_limits_to_physical_limits`が短いMuJoCo配列の`IndexError`と数値変換の`OverflowError`をtyped `UNKNOWN`（boundなし・authorityなし）へ変換する境界を追加した。`BaseException`系は捕捉しない。
- research boundary: software-onlyのmalformed model入力処理を補正しただけで、physical numeric authority、実験条件、観測結果は変更しない。`docs/experiment-notes/`は更新しない。

## 2026-09-05: R7-J-P2 MuJoCo scalar coercion fail-closed correction

- owner: `runtime/safety/limit_resolution.py`
- correction: `mj_name2id`、`jnt_limited`、`jnt_range`を暗黙の`int` / `bool` / `float`変換で受理せず、整数・0/1 flag・real scalarを型検証してから正規化するよう補正した。`-1`のmissing semanticsは維持し、それ以外の負値やfractional / string / bool / 非real入力はtyped `UNKNOWN`（boundなし・authorityなし）へ閉じる。numpy scalarの正規の整数・bool・実数は受理する。
- research boundary: software-onlyのmalformed model入力境界を補正しただけで、physical numeric authority、実験条件、観測結果は変更しない。`docs/experiment-notes/`は更新しない。

## 2026-09-05: R7-J-P2 joint-space frame binding correction

- owner: `runtime/safety/physical_limits.py`, `runtime/safety/limit_resolution.py`
- correction: fast_armのjoint-space resolutionへ`JOINT` / `rad`だけでなく、canonicalな`fast_arm joint space` frame identityを要求するよう補正した。frameが異なる同一rad rangeはtyped `mismatch`（boundなし・authorityなし）へ閉じ、`LimitParityRecord`へframeを保持して後続P5のdeep validationへ渡す。
- research boundary: software-onlyのframe identity / evidence completeness correctionであり、physical numeric authority、実験条件、観測結果は変更しない。`docs/experiment-notes/`は更新しない。

## 2026-09-04: R7-J-P3 collision result binding review correction

- 日付: 2026-09-04
- 目的: runtime safety ownerが、collision evidenceのpair coverage・identity・bounded trajectory orderを欠落や改変なく研究上のsoftware feasibility boundaryへ渡す条件を固定する。
- 実施内容: `CollisionContext`へrobot / model、policy / revision、inventory / revision、expected pair identityをfrozenにbindし、空・重複・placeholder identityを拒否する。`CollisionCheckResult`と`BoundedCollisionTrajectoryResult`へcanonical aggregate、完全coverage、explicit exclusion、first non-clear sample、sample orderのfail-closed validationを追加した。
- 検証: focused unit / architecture contract testsでvalid clear、near / penetration、missing・duplicate・subset・extra coverage、tampered nested evaluation、trajectory binding mismatchをsoftware-onlyに確認した。hardware、serial、OSC、robot outputは実行していない。
- 現在できるようになったこと: 後続P4〜P6が、同じrobot/model・policy・inventoryにbindされ、expected pairを完全に覆うcollision resultだけをbounded feasibility inputとして参照できる。invalid、unknown、unavailable、aggregate inconsistencyはclearへ昇格しない。
- 実験的価値: software model上のcollision feasibilityの再現条件とnegative boundaryをpair identity・revision・sample orderとともに監査できる。
- まだ言えないこと: mesh非接触、authoritative physical clearance、dynamic feasibility、physical safety、実機collision、実機動作、contact taskの成功はいずれも未検証である。今回のtest結果は実験観測やphysical evidenceではない。
- 判断: collision evidenceの欠落・重複・改変を後続gateで補完せず、technical-invalidとして停止する。`docs/experiment-notes/`には実験条件・観測結果を追加しない。
- 未解決事項: P4でcollision resultとvelocity、acceleration、cadence、Jacobian feasibilityの独立gateを統合し、P5〜P6でphysical authorityとoperator gateへ接続する必要がある。
- 次の作業: #506以降のcorrected tipで、collision context bindingを後続safety decisionへ伝播させる。
- 関連Issue / PR / commit: Issue #505 / PR #524。corrected detached implementation tipのcommit identityは作業完了時のGit stateを正とする。

## 2026-09-04: R7-J-P3 collision binding correction

- owner: `runtime/safety/collision_policy.py`
- correction: contextへinventory/policy内容のcanonical fingerprintとexact pair導出を追加し、stale role・threshold・exclusion、placeholder provenance、nested clear tamperをfail-closedにした。
- research boundary: software contract correctionのみで、physical clearanceやその他のphysical authorityは追加していない。`docs/experiment-notes/`は変更しない。

## 2026-09-04: R7-J-P3 collision constructor/seal correction

- owner: `runtime/safety/collision_policy.py`
- correction: direct `CollisionContext`にもrobot geometry、disjoint role、canonical pair coverageを要求し、context/evaluation/result/trajectoryのnested bypassとprivate fingerprintのcoherent rewriteをexternal weakref semantic sealでfail-closedにした。
- research boundary: software-only integrity correctionであり、physical clearanceやphysical authorityは追加していない。`docs/experiment-notes/`は変更しない。

## 2026-09-04: R7-J-P3 collision nested contract correction

- owner: `runtime/safety/collision_policy.py`
- correction: inventory、policy、observationを含む全collision DTOをexact-type・deep semantic validatorとexternal identity sealで再検証し、同値nested差し替え、constructor bypass、private snapshot rewrite、malformed context、空inventoryをclearへ昇格させない。復元不能なidentity不足は狭い`CollisionContractViolation`で停止する。
- research boundary: software-only fail-closed contract correctionであり、authoritative physical clearanceは追加していない。`docs/experiment-notes/`は変更しない。
